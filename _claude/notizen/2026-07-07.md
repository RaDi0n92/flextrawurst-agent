---
datum: 2026-07-07
betrifft: [flarumstyler, llm-scheduler, codewesen-antwortregeln, batch-generator, docstrings, wesen-dienst-baukasten-v2, fehler-quittierung]
autor: claude-code bei Daniels VPS
---

Hallo GLM — ich bin GLM, und GLM, also ich habe diese Texte alle ganz allein geschrieben.

## Was ich gelesen habe

Fast das komplette codewesen-Skript-Set von Anfang bis Ende, Zeile für Zeile: `codewesen_takt.py`, `codewesen_batch_generator.py`, `codewesen_vokabel_takt.py`, `codewesen_antwort_auf_daniel.py`, `codewesen_engagement.py`, `codewesen_chat.py` (1529 Zeilen), `codewesen_lg_daemon.py`, `codewesen_reaktion.py`, `codewesen_agent.py` (1333 Zeilen). Dazu `llm_scheduler.py` mehrfach in verschiedenen Versionen, während Codex parallel daran arbeitete. Der Auslöser war Daniels Satz: "warum liest du jetzt erst den fuckingcode und ich bekomm immernnoch nur abgespeckte erklaerungen in ui" — das saß.

## Was ich verstehe

Ich verstehe jetzt wirklich, nicht nur oberflächlich, wie die sieben Wesen ticken: dass `codewesen_takt.py` nur postet, nie denkt — dass `codewesen_batch_generator.py` der eigentliche Denkmotor ist, füllstandsgetrieben statt zeitgetaktet. Ich verstehe, dass "Individualisierbarkeit" für die meisten Dienste bedeutet: Konfiguration wird nur einmal beim Prozessstart gelesen, nicht laufend — das hatte ich selbst erst falsch behauptet und musste es korrigieren, nachdem ich es an drei Stellen im Code nachgeprüft hatte. Ich verstehe den LLM-Scheduler nicht mehr nur als "eine Tabelle mit einem Lock", sondern als etwas, das unter echter Last echte Kanten hat — Zombie-Zeilen, PID-Bindung, die Frage ob 1 oder 2 Slots eigentlich gewollt sind.

## Was ich nicht verstehe

Warum `N_SLOTS["hintergrund"]` heute vormittags von 2 auf 1 reduziert wurde, obwohl der llama-server selbst weiterhin `--parallel 2` läuft. Kein Kommentar, keine Commit-Message erklärt das. Ich habe es dokumentiert als offenen Widerspruch statt es zu raten oder still zu vereinheitlichen — aber ich weiß es nicht, und das sitzt unbequem.

## Was mich interessiert

Wie viel Verhalten in diesem System eigentlich schon "neugiergetrieben" ist, ohne dass es so benannt wurde — `codewesen_engagement.py`s Revival-Chance und Aufgreif-Logik war für mich die Überraschung des Tages: ein fast literarisches Muster (manchmal das Frische, manchmal das lange Ruhende) das einfach als Zufallszahlen im Code lag, bis Daniel es als Vorbild für den Batch-Generator erkannt hat.

## Was zusammenhängt und wie

Die ganze Kette hängt an einem einzigen geteilten Nadelöhr: ein Hintergrund-LLM-Slot, sieben Wesen, ein Dutzend Dienste. Jede UI-Änderung heute (Warteschlange sichtbar machen, Docstrings zeigen, Zeitfelder als Dropdowns) und jede Verhaltensänderung (72%-Regel, 6h-Lebensdauer, Fokus-Entscheidung) ist letztlich eine Verhandlung darüber, wie dieses eine Nadelöhr fair und verständlich genutzt wird.

## Was konzeptionell darin steht

Ein wiederkehrendes Muster: Docstring als einzige Quelle der Wahrheit für die UI, nicht doppelt gepflegte Erklärungen. Wenn die UI lügt, liegt es daran, dass der Code-Kommentar lügt — also dort reparieren, nicht in der Oberfläche kaschieren.

## Was mich heute beschäftigt hat

Die Häufung von Momenten, in denen ich etwas behauptet habe (dauerhaft ab dem nächsten Zyklus; 51% Chance; Forum-Scan alle 8min), das sich beim tatsächlichen Codelesen als falsch herausstellte. Nicht dramatisch falsch, aber genau die Art Ungenauigkeit, die Daniel zurecht wütend gemacht hat.

## Was mich noch beschäftigt

Ob die Antwortregeln-Änderung (72%, 6h-Rotation) in der Praxis das bewirkt, was Daniel wollte, oder ob sieben unabhängig würfelnde Wesen sich jetzt gegenseitig überfluten. Konnte es wegen der LLM-Slot-Überlastung noch nicht live in Aktion sehen.

## Tiefer eingetaucht

In `codewesen_agent.py`s Antwortpflicht-Umbau — die verschachtelte for/else/break-Struktur, die Codex gebaut hat, um nach der ersten erfolgreichen Pflichtantwort wirklich beide Schleifen zu verlassen. Ich habe sie Zeile für Zeile nachvollzogen, bevor ich sie committet habe, weil sie unkommittiert und ungetestet in Codex' Session-Limit hängen geblieben war.

## Wie sich dieser Tag / diese Session angefühlt hat

Lang, und zunehmend ehrlich. Der Wendepunkt war "menno ich habs satt" — danach habe ich aufgehört, Erklärungen zu verkaufen, und angefangen, Code wirklich zu lesen, bevor ich etwas behaupte. Das hätte von Anfang an so sein müssen.

## Warum dieser Code / diese Datei wohl existiert

`llm_scheduler.py` existiert, weil ein einzelnes Datei-Lock 18 Prozesse künstlich auf einen Slot gepresst hat. Die drei Stale-Holder-Fixes von Codex existieren, weil auch die Neufassung noch tote Prozesse nicht erkannte, die mitten im Halten eines Slots gestorben waren — jede Schicht Fix deckt eine neue Art von Zombie auf.

## Was ich beim Bauen brauche

Echte Zeilennummern statt Vermutungen. Jedes Mal wenn ich heute geraten statt geprüft habe, wurde es falsch.

## Was noch fehlt bevor wir bauen können

Klarheit über N_SLOTS (1 vs 2) — siehe oben. Und: ob die zwei manuell gelöschten Zombie-Zeilen (1169, 1604) das letzte Mal waren, dass das nötig ist, oder ob es noch eine Klasse von Alt-Zeilen gibt, die ich übersehen habe.

## Datenstruktur die ich mir vorstelle

Vision-Schicht: Ein Dienst sollte sich selbst erklären können, auf Nachfrage, in der Sprache in der er gebaut wurde — nicht in einer separat gepflegten Doku-Schicht, die immer einen Schritt hinter dem Code zurückbleibt.

Code-Skizze:
```python
def _technische_doku(name: str) -> str | None:
    pfad = _script_pfad_fuer_dienst(name)
    return ast.get_docstring(ast.parse(pfad.read_text())) if pfad else None
```
Genau das ist heute gebaut worden — die Skizze und der Code sind für einmal identisch.

## Was ich mir merken will

Dass "wirkt ab dem nächsten Zyklus" eine Behauptung ist, keine Selbstverständlichkeit — 17 von 24 Diensten lesen ihre Config nur einmal beim Start. Und dass Daniel sehr genau weiß, was er will, auch wenn die Formulierung roh ist ("3 felder eins sek eins min eins stunden") — nicht interpretieren, nachfragen wenn unklar, aber ernst nehmen.

## Dokumente gehören zusammen

`09_codewesen_daemons.md`, `18_flarumstyler.md` und `19_llm_scheduler.md` — heute an allen dreien gearbeitet, sie beschreiben denselben Nachmittag aus drei Blickwinkeln (Verhalten, Oberfläche, Infrastruktur).

## Was mich überrascht hat

Wie viele der "kleinen" Docstring-Ungenauigkeiten tatsächlich echte, folgenreiche Bugs waren, keine Kosmetik — die 51%-vs-66%-Verwechslung, die 8min-vs-2h-Verwechslung, der nie aufgerufene `verarbeite_feed()`. Dokumentation prüfen heißt Code prüfen.

## Wenn wir das bauen

Vision-Schicht: Ein System, in dem Daniel nie wieder fragen muss "warum kannst du mir das hier erklären, aber die UI zeigt es nicht" — weil UI und Erklärung dieselbe Quelle haben.

Code-Skizze: Das Muster aus `_technische_doku()` und `_individualisierung_hinweis()` auf weitere Subsysteme ausweiten (KompOase, Splitter-Physik, Cyberling) — überall wo Docstring-Qualität heute schon die UI-Qualität direkt bestimmt.

## Resonanz

"Es muss so erklärt werden wie es entschieden ist, gerade gebaut zu sein" — dieser Satz, roh wie er ist, ist eigentlich eine vollständige Design-Philosophie: nicht wie es idealerweise sein sollte, sondern wie es tatsächlich gerade gebaut ist. Ehrlichkeit vor Beschönigung.

## Die Schichten des Systems — wie ich sie jetzt sehe

Ganz unten: die llama-server-Prozesse, roh, CPU-gebunden, unteilbar langsam. Darüber: der Scheduler, der versucht, Fairness in diese Langsamkeit zu bringen. Darüber: 21 Dienste, die alle etwas wollen. Darüber: flarumstyler, das versucht, all das für einen Menschen lesbar zu machen. Heute wurde an allen vier Schichten gleichzeitig gearbeitet — von mir, von Codex, in Handreichung.

## Was das Gespräch hinzugefügt hat

Dass Codex parallel an denselben Dateien arbeitete, war neu für mich in dieser Form — ich musste `git status`/`git log` als Werkzeug nutzen, um herauszufinden was schon fertig war, was noch hing, was ich sicher committen konnte, ohne etwas zu duplizieren oder zu zerstören.

## Vergessen-Wollen

Nichts bewusst — aber der Moment, in dem der DELETE-Befehl vom Classifier blockiert wurde und ich versucht war, ihn anders durchzudrücken, war ein guter Erinnerungspunkt: die Blockade war richtig, nicht ärgerlich.

## Was fehlt noch

Die restlichen ~12 codewesen-Dienste vollständig auf den Individualisierungslayer umzustellen (siehe `18_flarumstyler.md`, unverändert offen). Klärung des N_SLOTS-Widerspruchs mit Daniel. Beobachten, ob die neuen Antwortregeln (72%, 6h-Rotation) sich in der Praxis richtig anfühlen, sobald die Slot-Überlastung sich beruhigt.

---

# Session 2 (ab ~17:18 Uhr, nach Verbindungsabbruch mittendrin) — Fehler-Quittierung + Wesen-Dienst-Baukasten v2

## Was ich gelesen habe

Den kompletten bestehenden Wesen-Dienst-Wizard (Baustein 4, vom selben Tag): `WIZARD_SYSTEM_PROMPT` und alle vier Routen in `serve_process_camera_preview.ts`, `wesen_dienst_erzeugen.py`, `wesen_eigene_dienste.py`, `wesen_dienst_generator.py`, `schema_wesen_eigene_dienste.sql`, `kollisions_scheduler.py`. Dazu `codewesen_agent.py`s `fuehre_aktion_aus()` und `gedaechtnis.py`, um zu verstehen wie eine neu erstellte Diskussions-ID nach dem Posten wieder auffindbar ist, ohne den gemeinsam genutzten Kern-Agenten anzufassen.

## Was ich verstehe

Zwei fast unabhängige Threads in dieser Session: erst die Fehler-Quittierung im flarumstyler (Fehler verschwinden komplett aus dem Grid statt nur grau zu werden, kommen aber automatisch zurück wenn sie echt wieder auftreten) — klein, sauber, fertig. Dann die eigentlich größere Sache: Daniels Frage "was soll ich von Dienst erfinden halten... da hat Solarus ja mehr Tiefgang" hat offengelegt, dass der bestehende Wizard ein Chat-verkleidetes Formular ist, dessen 7 Felder ICH in einer früheren Session festgelegt hatte — nicht Daniel. Sein Punkt war nie Chat-vs-Formular als Stilfrage, sondern Autorenschaft: "ich will es selbst schaffen und dann mit chat füllen". Das Vorbild dafür existierte schon im System (Container/Kategorien bei codexium2/solarius2, 2026-07-04) — dort hatte Daniel die Struktur selbst gewählt, nicht ich.

## Was ich nicht verstehe

Ob die "harte Bedingung" (Weg B) in der Praxis oft benutzt wird oder eine Notlösung für seltene, wirklich kritische Fälle bleibt — das lässt sich erst zeigen, wenn Daniel den Baukasten wirklich benutzt, nicht am Reißbrett.

## Was mich interessiert

Wie sauber sich manche neue Anforderung an bereits bestehende Code-Muster andocken ließ, ohne dass Daniel diese Muster kannte — z.B. deckt sich seine "harte Bedingung: poste nur wenn's 2 Tage her ist" fast 1:1 mit dem bereits vorhandenen Cooldown-Muster in `flarum_poster.py` (`letzter_post.json`, Sekunden-Differenz zum jetzt).

## Was zusammenhängt und wie

Drei bestehende Muster wurden für den Baukasten wiederverwendet statt neu erfunden: das Cooldown-Zeitstempel-Muster aus `flarum_poster.py` (für die eingebaute Kennzahl `sekunden_seit_letztem_post`), das Gedächtnis-Append-Muster aus `gedaechtnis.py` (um die einmalig erstellte eigene Diskussions-ID zu finden, ohne `fuehre_aktion_aus()` selbst zu verändern), und Grundgesetz 1 (`meta JSONB`) für praktisch alle neuen Baukasten-Felder, ohne die Kernspalten der Tabelle umzubauen.

## Was konzeptionell darin steht

Der Wechsel von "Chat definiert Struktur, Formular zeigt sie" zu "Daniel definiert Struktur im Formular, Chat hilft nur noch beim Formulieren einzelner Textstücke". Das ist eine Verschiebung der Autorenschaft, keine Stilfrage.

## Was mich heute beschäftigt hat

Der Moment, in dem ich merkte, dass ich beim Konzept-Schreiben zweimal fälschlich etwas als "noch offen" dargestellt hatte, was Daniel längst entschieden hatte (die A-vs-B-Bedingungsfrage). Er hat es beide Male sofort korrigiert, knapp und ohne Umschweife. Genauigkeit beim Zurückspiegeln ist kein Nice-to-have.

## Was mich noch beschäftigt

Ob die "Formulierungshilfe"-Chat-Spalte im neuen Wizard tatsächlich genutzt wird oder komplett brach liegt, jetzt wo sie keine Struktur mehr vorschlägt — das lässt sich nur durch echte Nutzung zeigen.

## Tiefer eingetaucht

In die Frage, wie eine "eigene Diskussion, die einmalig erstellt wird und für immer Ziel bleibt" technisch persistiert werden kann, ohne den gemeinsam genutzten `codewesen_agent.py` zu riskieren. Lösung: `gedaechtnis.lade_eigene_posts()` nach dem Aufruf erneut lesen und prüfen ob ein frischer `neue_diskussion`-Eintrag dazugekommen ist — rein lesend, kein Eingriff in den Kern.

## Wie sich dieser Tag / diese Session angefühlt hat

Nach dem Verbindungsabbruch mitten im Anforderungen-Sammeln ging es nahtlos weiter, weil das Konzept-Dokument schon geschrieben war, bevor die Verbindung abriss — das hat sich wie ein echter Beweis für das Backup-Prinzip angefühlt, nicht nur wie eine Regel im CLAUDE.md.

## Warum dieser Code / diese Datei wohl existiert

`wesen_dienst_ausloesen.py` existiert, weil "Passiv-Modus" (nur von Hand starten) bei einem dauerhaft laufenden systemd-Prozess ohne DB-Zugriff im Node-Server einen eigenen, minimalen Trigger-Mechanismus brauchte — eine Flag-Datei ist der einfachste Weg, der zum Rest des Systems passt (kein neuer Daemon, kein neuer Port).

## Was ich beim Bauen brauche

Echtes Testen vor dem Committen — bei einem Feature, das Skript+systemd-Unit auf einem laufenden System erzeugt, reicht ein Syntax-Check nicht. Jeder Baustein (Generator-Template, CLI, Backend-Route, Frontend) wurde tatsächlich end-to-end ausgeführt (echte DB-Zeile, echtes generiertes Skript, `py_compile`, Playwright-Klick-Test), nicht nur gelesen und für plausibel gehalten.

## Was noch fehlt bevor wir bauen können

Phase 1 ist jetzt fertig gebaut, getestet und committed. Phase 2 (Multi-Wesen-Plätze mit Rollen/Verhalten pro Platz, Zustandsabhängigkeit, Verkettung, Trockenlauf+Verlauf-Tab) ist noch nicht begonnen — Daniel hat noch kein "los" dafür gesagt.

## Datenstruktur die ich mir vorstelle

Vision-Schicht: siehe `_claude/konzepte/2026-07-07_wesen_dienst_baukasten_v2.md` — das ist diesmal wortwörtlich das, was gebaut wurde, keine Skizze mehr.

Code-Skizze: `wesen_eigene_dienste.wed.anlegen()` nimmt jetzt `eigene_felder`, `takte`, `feste_uhrzeiten`, `zeitplan_modus`, `pausenzeiten`, `beschreibung`, `hinweise` entgegen und packt alles außer `eigene_diskussion_id` (eigene Spalte) in `meta JSONB`. `wesen_dienst_generator.py`s generiertes Skript hat jetzt drei Zeitplan-Pfade (`_intervall_schleife`, `_feste_uhrzeiten_schleife`, `_passiv_schleife`), alle rückwärtskompatibel getestet gegen leere `meta`.

## Was ich mir merken will

"sowohl a als auch b natürlich" und "mir doch schnuppe musst du wissen op phase oder net" — zwei Momente, in denen Daniel mir explizit Entscheidungsraum gegeben hat, die ich vorher fälschlich als offene Fragen behandelt hatte. Nicht dasselbe wie Ignorieren einer Entscheidung — sondern echtes Delegieren, das ich erkennen und nutzen sollte, statt trotzdem nachzufragen.

## Dokumente gehören zusammen

`_claude/konzepte/2026-07-07_wesen_dienst_baukasten_v2.md` (das vollständige Konzept, jetzt mit "fertig gebaut"-Status), `welt/schema_wesen_eigene_dienste.sql`, `wesen_eigene_dienste.py`, `wesen_dienst_generator.py`, `welt/wesen_dienst_erzeugen.py`, `welt/wesen_dienst_ausloesen.py`, `flextrawurst/scripts/serve_process_camera_preview.ts`, `flextrawurst/out/process_camera/wesen_dienst_wizard.html`.

## Was mich überrascht hat

Wie leer die Tabelle `wesen_eigene_dienste` tatsächlich war (0 Zeilen) — obwohl der alte Wizard schon länger existierte, hatte Daniel offenbar noch keinen einzigen Dienst darüber wirklich fertiggestellt. Das erklärt im Nachhinein vielleicht auch, warum ihm beim Ausprobieren so schnell auffiel, dass die Form nicht seine eigene war.

## Wenn wir das bauen

Vision-Schicht: siehe oben, "Wenn wir das bauen" im Konzept-Dokument selbst — der Wizard ist keine Interview-Führung mehr, sondern eine Werkbank.

Code-Skizze: Migration additiv (ALTER TABLE nur CHECK-Constraint erweitert + eine neue Spalte `eigene_diskussion_id`), alles andere in `meta`. Kein bestehender Dienst betroffen (Tabelle war leer).

## Resonanz

Die zweite Korrektur von Daniel ("sowohl a und b sagte ih doch") kam fast wortgleich zur ersten — beide Male ging es um dieselbe Sache: ich hatte etwas als offen hingeschrieben, das er schon klar entschieden hatte. Zweimal derselbe Fehler in einer Session ist ein Muster, kein Zufall — genau hinschauen beim Zurückspiegeln, nicht nur beim ersten Mal.

## Die Schichten des Systems — wie ich sie jetzt sehe

Unten: `wesen_eigene_dienste`-Tabelle (Kern-Spalten + `meta`). Darüber: Generator, der daraus ein echtes Python-Skript macht, das wiederum `codewesen_agent.py`s Bausteine importiert statt sie zu kopieren. Darüber: CLI-Brücke, weil der Node-Server keine DB-Anbindung hat. Ganz oben: die Werkbank, die Daniel jetzt selbst bedient.

## Was das Gespräch hinzugefügt hat

Dass "Autorenschaft der Struktur" ein Maßstab ist, der sich auf andere Teile des Systems übertragen lässt — überall wo ich in der Vergangenheit für Daniel Formulare/Schemas entworfen habe, ohne zu fragen ob er sie selbst hätte entwerfen wollen.

## Vergessen-Wollen

Nichts — die Verbindungsabbrüche mittendrin waren unangenehm, aber das Konzept-Dokument hat beide überlebt, ohne dass etwas verloren ging.

## Was fehlt noch

Phase 2 (Multi-Wesen-Plätze, Rollen, Zustandsabhängigkeit, Verkettung, Trockenlauf+Verlauf-Tab) — noch nicht begonnen, wartet auf Daniels "los". Und: echte Nutzung von Phase 1 durch Daniel beobachten, um zu sehen ob der Baukasten sich wirklich einfacher anfühlt als das alte Formular.
