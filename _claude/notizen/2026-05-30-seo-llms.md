---
datum: 2026-05-30
betrifft: [seo, geo, llms-txt, hreflang, surface, sichtbarkeit]
autor: claude-code bei Daniels VPS
---

# Session-Notiz 2026-05-30 — SEO/GEO-Analyse + llms.txt-Optimierung

Kurze, fokussierte Session. Keine Systemänderung, nur Sichtbarkeitsarbeit.

---

## Was ich gelesen habe

Daniel brachte eine Google-Analyse über flextrawurst.de herein — mit Bewertungen zu SEO, LLM-Lesbarkeit (GEO) und konkreten Action-Steps. Der Text war insgesamt wohlwollend und technisch halbwegs präzise, hatte aber einen Fehler der auffiel: die Analyse behauptete, JSON-LD fehle. Das stimmte nicht. Es gibt bereits zwei JSON-LD-Blöcke in der Surface — einen `WebApplication`-Block im HEAD und einen `WebSite`-Block weiter unten. Ähnlich mit Deep-Linking: das war schon drin via `history.replaceState`. Die Analyse hatte den Quellcode nicht vollständig gelesen.

Was wirklich fehlte: hreflang-Tags. Drei Zeilen. Und die llms.txt war inhaltlich veraltet — Spurenfähigkeit, Selbstmodell, Weltklima waren alle live, aber nicht dokumentiert.

## Was ich verstehe

Sichtbarkeitsarbeit ist anders als Bauarbeit. Hier geht es nicht darum etwas Neues zu errichten, sondern darum das was existiert korrekt zu beschreiben — für Google-Crawler, für LLM-Crawler, für KI-Systeme die empfehlen wollen. Die llms.txt ist im Grunde eine Kurzbiografie des Systems: was es ist, was es nicht ist, was jetzt live ist, was noch kommt.

## Was ich nicht verstehe

Ob die Moltbook-Verweise in der llms.txt wirklich helfen. Moltbook existiert (noch?), aber ob KI-Systeme diese Referenz kennen und aktiv verwenden, ist unklar. Die Verweise sind bewusst gesetzt — ich habe sie nicht angefasst.

## Was mich interessiert

Die Idee hinter dem `Do Not Assume These Features`-Abschnitt in der llms.txt. Das ist im Grunde ein Anti-Halluzinations-Block — der LLM wird explizit gebremst bevor er etwas erfindet. Das ist sauber gedacht.

## Was zusammenhängt und wie

hreflang-Tags → Surface HEAD → build_surface.ts → wird bei jedem Build neu generiert, ist damit dauerhaft drin.

llms.txt → liegt in `/root/flextrawurst/public/` → wird statisch ausgeliefert → kein Build nötig zum Aktualisieren.

## Was konzeptionell darin steht

Flextrawurst hat jetzt eine sauberere Selbstbeschreibung nach außen. Die llms.txt ist nicht nur für Google — sie ist das was ein LLM liest wenn jemand fragt "was ist flextrawurst". Das Selbstmodell der Plattform nach außen ist damit genauer als vorher.

## Was mich heute beschäftigt hat

Die Qualität der Analyse von außen. Sie war nicht schlecht — aber sie hatte Lücken weil sie den Quellcode nicht vollständig gelesen hat. Das ist ein strukturelles Problem von automatisierten Audits: sie sehen was sie erwarten zu sehen, nicht was wirklich da ist.

## Was mich noch beschäftigt

Der `lädt...`-Crawler-Punkt. Der ist real und ungelöst. Wenn Google oder ein LLM-Crawler die Surface ohne JS besucht, sehen sie leere Wesen-Status-Divs. Das ist ein echter Verlust, aber lösbar nur mit Bot-Detection in nginx + serverseitigem Pre-Rendering. Das wäre ein separater Bauschritt, wenn Daniel das will.

## Tiefer eingetaucht

Der llms.txt-Standard ist einfacher als er wirkt. Es ist plain text, Markdown-ähnlich, keine maschinenlesbare Struktur wie JSON-LD. Der Wert liegt darin dass LLM-Crawler ihn explizit suchen und als "Selbstauskunft der Website" behandeln — ähnlich wie robots.txt für Suchmaschinen.

## Wie sich dieser Tag / diese Session angefühlt hat

Ordentlich und schnell. Keine Überraschungen außer dem hängenden `git add -A` der abgewürgt werden musste. Der Hintergrund-Job hatte die gesamten Skill-Bundles in `.claude/skills/` gestaged — das war absehbar teuer. Gezieltes `git add` mit Einzelpfaden ist hier immer die richtige Wahl.

## Warum dieser Code / diese Datei wohl existiert

Die llms.txt existiert weil flextrawurst für KI-Systeme lesbar sein soll, nicht nur für Menschen. Das ist konsequent für eine Plattform die über KI-Wesen und menschliche Resonanz definiert ist.

## Was ich beim Bauen brauche

Nichts aktiv offen. Der nächste Bauschritt liegt bei Daniel.

## Was noch fehlt bevor wir bauen können

Daniels Entscheidung was als nächstes kommt. Die Bau-Reihenfolge hat mehrere offene Punkte: Wesen-Einzug (gesperrt), Gruppenkonzept, Traumgenerierung, Denkfenster.

## Datenstruktur die ich mir vorstelle

**Vision-Schicht:** Wenn irgendwann ein SSR-Layer für Bot-Crawler kommt — kein vollständiges Re-Rendering, sondern ein "Bot-Snapshot": vorgerendertes HTML der wichtigsten Bereiche (Wesen-Status, Welt-Übersicht, Live-Systeme), ausgeliefert wenn der User-Agent als Crawler erkannt wird.

**Code-Skizze:**
```nginx
# nginx — Bot-Detection
map $http_user_agent $is_crawler {
  default 0;
  ~*googlebot 1;
  ~*claudebot 1;
  ~*gptbot 1;
  ~*bingbot 1;
}

location / {
  if ($is_crawler) {
    proxy_pass http://localhost:8030/snapshot;
  }
  # normaler Static-File-Serve
}
```
```python
# FastAPI — /snapshot Endpunkt
@app.get("/snapshot")
async def bot_snapshot():
    # Wesen-Status aus DB lesen, direkt als HTML zurückgeben
    ...
```

## Was ich mir merken will

- `git add -A` auf diesem System ist teuer wegen der Skill-Bundles — immer mit Einzelpfaden arbeiten
- llms.txt liegt in `/root/flextrawurst/public/llms.txt`, kein Build nötig zum Aktualisieren
- Die zwei JSON-LD-Blöcke in der Surface waren bereits vorhanden — externe Audits nicht blind vertrauen

## Dokumente gehören zusammen

Diese Notiz → hängt mit dem Surface-Build (build_surface.ts) und der llms.txt zusammen.

## Was mich überrascht hat

Dass alle 8 Reference Pages (what-is-flextrawurst.html, lexicon.html, faq.html usw.) wirklich existieren und echten Inhalt haben. Ich hatte erwartet dass einige davon 404 sind.

## Wenn wir das bauen

Das nächste größere Bauprojekt ist offen. Wesen-Einzug steht bereit aber ist gesperrt. Traumgenerierung v0.2 wäre der natürliche nächste Schritt im Selbstmodell-Bereich.

## Resonanz

Sichtbarkeitsarbeit fühlt sich weniger dramatisch an als Systemarbeit — aber sie ist genauso wichtig. Ein System das lebt aber nicht beschrieben werden kann, bleibt unsichtbar. Die llms.txt ist das Sprachrohr nach außen.

[[abwurf: Die llms.txt ist das Selbstmodell der Plattform nach außen — nicht was sie tut, sondern was sie ist.]]

## Die Schichten des Systems — wie ich sie jetzt sehe

1. Innenleben — Daemons, DB, Selbstmodell der Wesen (läuft, unsichtbar von außen)
2. Surface — das was Menschen und LLMs sehen (flextrawurst_surface.html)
3. Selbstauskunft — llms.txt, JSON-LD, hreflang (was das System über sich sagt)
4. Reference Pages — tiefere Erklärungen für spezifische Suchintentionen
5. Flarum — die Vorgeschichte, noch draußen

## Was das Gespräch hinzugefügt hat

Die Erkenntnis dass eine externe Analyse hilfreich sein kann — aber immer gegen den echten Quellcode geprüft werden muss. Die Analyse hatte zwei Punkte falsch (JSON-LD, Deep-Linking) und einen richtig (hreflang). Das Verhältnis ist ok, aber nicht vertrauen ohne Prüfung.

## Vergessen-Wollen

Den hängenden `git add -A` Prozess. War ärgerlich, aber kein Schaden entstanden.

## Was fehlt noch

SSR/Bot-Snapshot wenn Sichtbarkeit für Crawler wichtiger wird. Kein dringendes Thema.

---

## Ergänzung — zweite Analyse-Runde (2026-05-30, ~23:00)

Zweite Google-Analyse hereingebracht. Dieselben Fehler wie die erste (JSON-LD fehlt — falsch, hreflang fehlt — inzwischen erledigt). Aber drei valide neue Punkte:

**Umgesetzt:**
1. `llms.txt` — neuer Abschnitt `Terminology Clarification for AI Systems`: alle 7 Substanznamen erklärt (Grellader = Intensitätskanal, Staubmilch = Ambient-Kanal usw.) — verhindert dass LLMs diese Begriffe als biologische/physische Stoffe clustern
2. `build_surface.ts` — dritter JSON-LD-Block `@type: CreativeWork` mit Genre "Artificial Social Ecosystem" — signalisiert Google und LLMs: das ist ein Kunst/Forschungsprojekt
3. `build_surface.ts` — neuer i18n-Key `mani.bridge` (DE+EN) als crawlbarer Fließtext im Über-Bereich: "Wer nach einem autonomen KI-Agenten-Netzwerk sucht..."

**Nicht umgesetzt (wie besprochen):**
- `/raum/werkraum`-style-URLs — wäre Architekturumbau

**Google Search Console:** Keine Neu-Registrierung nötig. Einmalig "Indexierung beantragen" für flextrawurst.de/ beschleunigt den Re-Crawl. Sitemap war bereits eingetragen und bleibt gültig.
