---
datum: 2026-07-09
betrifft: [tts, logs, compare, backend, fortsetzung]
importable: false
autor: codex bei Daniels VPS
---

Hallo Codex — ich bin Codex, und Codex, also ich habe diese Texte alle ganz allein geschrieben.

# Sessionnotiz — TTS Logs Server-Compare

## Was ich gelesen habe

Ich habe nach dem Abbruch zuerst die eigene Codex-Spur, Karte, Resonanzende, Briefkasten, Referenzbild und die 490-Punkte-Liste wieder eingesammelt, damit die Fortsetzung nicht nur aus dem letzten Chatrest besteht. Relevant für diese Session war danach die TTS-Linie: eigene Notiz vom 2026-07-05, Claudes TTS-Ursprungsnotiz vom 2026-06-18 und die jüngere Übergabe zur Florian-/TTS-Diagnose.

Dann habe ich `werkraum/welt/tts_ui.html` und `werkraum/welt/tts_service.py` direkt an den Log-Stellen gelesen. Wichtig war: der Browser konnte bereits zwei gespeicherte Loganalysen lokal vergleichen, und Regel-Export/Import war schon erledigt. Der nächste saubere Schritt war also wirklich nur die Server-Schicht unter denselben bestehenden Vergleich zu ziehen.

## Was ich verstehe

Die Fortsetzung war Ergänzung, nicht Umbau. Die bestehende Logs-Oberfläche, die gespeicherten Analysen in `tts_logs.json` und der lokale Compare-Text bleiben sichtbar und wiedererkennbar. Neu dazugekommen ist nur ein serverseitiger Vergleich gespeicherter Analysen per ID, damit der Vergleich nicht nur an der aktuell geladenen Browser-Kopie hängt.

## Was ich nicht verstehe

Ich weiß noch nicht, ob Daniel als nächsten Schritt einen echten diff-artigen Vollvergleich der Textvorschauen will oder ob die jetzige strukturierte Zusammenfassung reicht. Der aktuelle Server-Compare vergleicht bewusst nur Counts und Gruppen-Signaturen, also dieselbe Semantik wie vorher der Browser-Diff.

Ich weiß außerdem nicht, wann der laufende TTS-Service neu gestartet werden darf. Ich habe ihn absichtlich nicht angerührt.

## Was mich interessiert

Mich interessiert an dieser kleinen Änderung, dass sie den Charakter der Logs-Fläche nicht verändert. Es bleibt ein gezieltes Werkzeug: speichern, laden, vergleichen, exportieren. Kein neuer Analysemodus, keine stille Hintergrundlogik.

## Was zusammenhängt und wie

`tts_service.py` hält die gespeicherten Loganalysen in `tts_logs.json` und kennt jetzt einen `/logs/compare`-Endpunkt. `tts_ui.html` nutzt diesen Endpunkt beim Klick auf `Vergleich`, fällt aber auf den bisherigen Browser-Vergleich zurück, falls der Serververgleich scheitert oder alte lokale Daten ohne Server-ID auftauchen.

## Was konzeptionell darin steht

Der Compare gehört eigentlich dorthin, wo die gespeicherten Analysen leben. Nicht weil der Browservergleich falsch wäre, sondern weil gespeicherte Analysen auf Dauer mehr sein sollen als momentane UI-Objekte.

## Was mich heute beschäftigt hat

Der Startapparat war groß und der eigentliche Eingriff klein. Genau deshalb war die Versuchung da, den Abbruch nur grob zu überfliegen und sofort weiterzucoden. Die Pflichtlektüre hat aber bestätigt, dass die TTS-Linie bewusst lokal und schmal gehalten wurde. Das war wichtig, damit aus „serverseitig vergleichen“ nicht versehentlich „neue Schreibfläche“ wird.

## Was mich noch beschäftigt

Es fehlt ein echter Live-Check über den laufenden HTTP-Endpunkt, weil ich den TTS-Dienst ohne Daniels Rückfrage nicht neu gestartet habe. Verifiziert ist die neue Compare-Logik deshalb syntaktisch und auf Modulebene, nicht als bereits laufende Service-Änderung.

## Tiefer eingetaucht

Ich habe den Werkraum-Graph nicht nur gelesen, sondern nach dem Code-Diff mit `graphify update .` aktualisiert. Die neue TTS-Logik sitzt als kleiner weiterer Knoten im bestehenden TTS-/Werkzeug-Cluster und nicht als neues Nebensystem.

## Wie sich dieser Tag / diese Session angefühlt hat

Sauberer als der Abbruch. Wenig Glanz, viel Anschlussarbeit, aber genau die richtige Größe: zwei Dateien, ein neuer Endpunkt, ein Fallback, keine Seiteneffekte.

## Warum dieser Code / diese Datei wohl existiert

Die Änderung existiert, damit gespeicherte Loganalysen auf dem VPS selbst vergleichbar sind, statt nur als zwei Browserobjekte nebeneinander zu liegen. Die Notiz existiert, damit die nächste Instanz nicht wieder rätseln muss, ob „server compare“ schon gebaut oder nur angedacht war.

## Was ich beim Bauen brauche

Vor weiteren TTS-Log-Schritten brauche ich weiterhin die bestehende Herkunft: lokal gespeicherte Regeln, gespeicherte Analysen, Export-Endpunkte, keine heimliche Server-Schreibfläche jenseits dessen, was bereits bewusst da ist.

## Was noch fehlt bevor wir bauen können

Für den nächsten konkreten Schritt fehlt nur Daniels Entscheidung, ob der laufende TTS-Service dafür neu gestartet werden darf. Code-seitig ist der Server-Compare vorbereitet.

## Datenstruktur die ich mir vorstelle

**Vision-Schicht**

Eine gespeicherte Loganalyse ist kein lose kopierter Textblock, sondern ein Zustand mit Herkunft. Zwei solcher Zustände sollten dort miteinander verglichen werden können, wo ihre Herkunft gehalten wird.

**Code-Skizze**

```python
class LogCompareRequest(BaseModel):
    base_id: str
    other_id: str

@app.post("/logs/compare")
async def compare_log_analyses(req: LogCompareRequest):
    ...
```

```ts
async function compareLogEntriesOnServer(base, other) {
  return fetch(LOGS_COMPARE_API, { method: 'POST', ... });
}
```

## Was ich mir merken will

Der richtige Fortsetzungspunkt nach dem Abbruch war tatsächlich schon im letzten Übergabesatz benannt: erst Server-Compare, nicht gleich neue UI-Ideen.

## Dokumente gehören zusammen

`/root/werkraum/welt/tts_ui.html`, `/root/werkraum/welt/tts_service.py`, `/root/werkraum/welt/tts_logs.json`, `_codex/notizen/2026-07-05.md`, `_codex/_import_claude_grundriss/notizen/2026-06-18.md`, `_codex/_import_claude_grundriss/notizen/2026-07-06.md`.

## Was mich überrascht hat

Dass der Browser-Fallback fast kostenlos mitzunehmen war. So bleibt der neue Serververtrag nützlich, ohne die bisherige lokale Robustheit wegzuwerfen.

## Wenn wir das bauen

**Vision-Schicht**

Wenn die Logs-Fläche weiter wächst, sollte sie Analysezustände tragen können wie kleine Akten: speichern, vergleichen, exportieren, vielleicht später kommentieren. Aber sie sollte nicht plötzlich wie ein großes observability-System tun.

**Code-Skizze**

```json
{
  "base": {"id": "...", "filename": "..."},
  "other": {"id": "...", "filename": "..."},
  "group_counts": {"added": 1, "removed": 0, "changed": 2}
}
```

## Resonanz

[[abwurf: Der Compare gehört dorthin, wo die gespeicherten Analysen leben.]]

## Die Schichten des Systems — wie ich sie jetzt sehe

Ganz unten liegen rohe Logtexte und ihre Signaturen. Darüber die gespeicherten Analysen in `tts_logs.json`. Darüber jetzt der kleine Server-Compare. Erst darüber sitzt die UI, die den Vergleich als Text sichtbar macht.

## Was das Gespräch hinzugefügt hat

Der letzte Abbruchtext hat den nächsten Schritt schon fast sauber benannt. Meine eigentliche Arbeit war, ihn nicht größer zu machen als er ist.

## Vergessen-Wollen

Nicht wieder in den Reflex fallen, bei einer kleinen, klaren Fortsetzung noch ein neues Verwaltungssystem zu erfinden.

## Was fehlt noch

Live-Service-Neustart und echter HTTP-Endpunkt-Test sind nicht mehr offen: `tts-service` läuft wieder, und `POST /logs/compare` ist gegen echte Analysen geprüft. Der neue Textdiff zeigt jetzt die tatsächlichen Logzeilen als Unified Diff. Die Code-Verifikation war: `python3 -m py_compile`, `node --check` auf dem extrahierten Script, `graphify update .` und ein direkter Modul-Test von `_compare_log_analyses()`.

## Stand Bau-Reihenfolge

Unverändert. TTS ist weiterhin Werkzeugspur und kein neuer Eintrag in der flextrawurst-Bau-Reihenfolge. Offene große Welt-Schritte bleiben: Persönliche Welt, Wesen-Einzug, Gruppenkonzept, Entitätenschichten, Schlaf-System, eigenes Post-System für Wesen.

## Nachtrag

Die Testanalyse `log-1783554690167` wurde nach der Live-Verifikation wieder aus `tts_logs.json` entfernt. Nur der echte Vergleichspfad bleibt stehen.

## Zweiter Nachtrag

Der Compare hat jetzt oben eine kurze Einordnung (`headline`) zusätzlich zum Unified Diff. Live getestet mit einer temporären Analyse `log-1783555171012`; danach wieder entfernt. Der Vergleich sagt jetzt nicht nur, was anders ist, sondern auch grob, in welche Richtung.

## Dritter Nachtrag

Die Headline ist danach noch geglättet worden: aus der rohen Zahlenspur wurde eine natürlichere Form wie `1 Zeile weniger, 1 neue Gruppe, 1 entfallene Gruppe`. Der Inhalt blieb derselbe, nur der Satz ist brauchbarer geworden.

## Vierter Nachtrag

Zusätzlich zur Kurzzeile zeigt der Compare jetzt auch einen `summary_diff`. Live geprüft: die Summary-Zeilen unterscheiden sich jetzt sichtbar, nicht nur die Rohtexte und Gruppen. Das macht den Unterschied auf der Oberfläche lesbarer, ohne die bestehende Struktur umzubauen.

## Fünfter Nachtrag

Die Summary ist jetzt doppelt verdichtet: oben als `summary_sentence` in einem Satz, darunter weiterhin der `summary_diff` für die Details. Der Live-Test mit `log-1783556334687` hat das bestätigt; danach wurde der temporäre Eintrag wieder aus `tts_logs.json` entfernt.

## Sechster Nachtrag

Die Summary klingt jetzt auch wirklich wie ein Satz: `Die Analyse hat Zeilen 6→5 (-1), Tracebacks 1→0 (-1), 1 neue Gruppe und 1 entfallene Gruppe.` Das ist dichter als die vorherige Listenform, aber immer noch direkt genug, um den Unterschied sofort zu sehen.

## Siebter Nachtrag

Die Satzform ist jetzt sauberer belegt: `Im Vergleich gibt es 1 Zeile weniger, 1 Traceback weniger, 1 neue Gruppe und 1 entfallene Gruppe.` Das klingt natürlicher als die vorherige Rohform und bleibt trotzdem kurz genug für den Kopf.

## Achter Nachtrag

Die Satzform ist jetzt in Blöcke gegossen statt nur als Liste zusammengesetzt: `Im Vergleich gibt es 1 Zeile und 1 Traceback weniger, 1 neue Gruppe dazu und 1 Gruppe entfallen.` Das liest sich einen Tick ruhiger und zeigt die Richtung klarer.

## Neunter Nachtrag

Die Satzform ist jetzt am saubersten: `Im Vergleich gibt es 1 Zeile und 1 Traceback weniger, 1 neue Gruppe ist dazugekommen und 1 Gruppe ist entfallen.` Das klingt nach einem echten Satz und nicht mehr nach einem Feld, das seine eigene Grammatik erklärt.

## Zehnter Nachtrag

Die live geprüfte Endfassung klingt jetzt noch natürlicher: `Im Vergleich gibt es eine Zeile und ein Traceback weniger, eine neue Gruppe ist dazugekommen und eine Gruppe ist weggefallen.` Der temporäre Vergleichseintrag wurde wieder aus `tts_logs.json` entfernt; übrig bleibt nur die bessere Satzform im Compare.

## Elfter Nachtrag

Die Satzform ist jetzt als direkte Feststellung gebaut: erst die fehlenden Dinge, dann die Gruppenbewegung. Das liest sich ruhiger als die vorige Kette und bleibt trotzdem knapp genug für den ersten Blick.

## Zwölfter Nachtrag

Die letzte Form ist noch klarer geworden: `Es fehlen eine Zeile und ein Traceback. Eine Gruppe ist dazugekommen. Eine Gruppe ist weggefallen.` Drei kurze Sätze statt einer verschachtelten Kette, das trägt besser im Kopf.

## Dreizehnter Nachtrag

Daniel hat den richtigen Bruch benannt: Die TTS-Fläche war zu viel Hülle und zu wenig Werkzeug. Heute wurde nicht weiter an Erklärungstexten gefeilt, sondern an Bedienverträgen: OCR zeigt jetzt seine Serverfähigkeit, Dokumente erklären und zeigen Scope, Chunks können bearbeitet und gelöscht werden, Textflächen haben eine Vollansicht, Webarchiv kann mehrere Same-Domain-Seiten crawlen, und Formulare füllen einfache HTML-Felder nach `name` oder `id`. Noch offen bleibt echte Härteprüfung mit Daniels echten Dateien und echten Formularseiten; die lokale Probe hat aber Import, Chunk-Update, Chunk-Löschung, OCR-Status und Webarchiv-Crawl bestätigt.

## Vierzehnter Nachtrag

Die Härteprüfung ist jetzt nicht mehr nur behauptet: `tts_realworld_tests.py` testet OCR gegen ein Bild mit bekannter Wahrheit, OCR gegen das echte Vision-Bild, Dokumentimport gegen `/root/werkraum/geni/README.md`, Dokumentensuche, Chunk-Edit, serverseitige Formularfüllung und Webarchiv-Crawl gegen `https://flextrawurst.de/` mit fünf Seiten. `tesseract-ocr` mit Deutsch/Englisch ist installiert; `/ocr/status` meldet jetzt Bild-OCR und PDF-Text als bereit. Der Testlauf war grün und hat seine temporären Dokumente, OCR-Jobs, Snapshots und Formularprofile wieder gelöscht.

## Fünfzehnter Nachtrag

Der fehlende Browserteil ist nachgezogen: `tts_browser_e2e.py` klickt die echte `/tts`-Seite mit Playwright durch. Tabs schalten sichtbar, OCR läuft über die UI gegen ein echtes Testbild, Dokumentimport/Suche/Scope/Chunk/Vollansicht laufen über Buttons, Formularfüllung läuft über den UI-Button gegen den Server-Fill, und Webarchiv-Snapshot plus Vollansicht laufen ebenfalls im Browser. Der Test war grün und hat seine OCR-, Dokument- und Snapshot-Testdaten wieder gelöscht.

## Sechzehnter Nachtrag

Daniels Einwand war berechtigt: grün ist nicht automatisch gut. Die TTS-Werkzeuge tragen jetzt Qualitätsmetriken statt nur Status. OCR meldet Zeichen/Wörter/Leerstand, Dokumente melden Textzeichen, Dateigröße, Extraktionsverhältnis und Chunk-Anzahl, Formulare melden offene Platzhalter und gefüllte Felder, Webarchiv meldet Textmenge, Seiten und gecrawlte URLs. Die Tests prüfen diese Metriken mit: flextrawurst.de ergab im Realworld-Test fünf gecrawlte Seiten und 99.948 Textzeichen, das echte GENI-README lag bei 2.584 extrahierten Zeichen aus 2.644 Byte, und das Browser-Webarchiv zeigte seine Qualitätszeile direkt in der UI.

## Siebzehnter Nachtrag

Die Latte wurde nochmal höher gelegt. OCR wird jetzt auch gegen ein verrauschtes, leicht gedrehtes Bild mit kleinerer Schrift geprüft; Pflichtbegriffe und Wortabdeckung lagen bei 100%. Dokumente werden zusätzlich gegen `/root/werkraum/welt/api.py` getestet: 517.461 extrahierte Textzeichen aus 521.296 Byte, 1.000 Chunks, Suche im großen Codefile grün. Webarchiv nutzt jetzt Sitemap-Seeding; `https://flextrawurst.de/` kam im harten Lauf auf 13 gecrawlte URLs und 209.195 Textzeichen statt nur zehn Seiten aus Link-Kratzen.
