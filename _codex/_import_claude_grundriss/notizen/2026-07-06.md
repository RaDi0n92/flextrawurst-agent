---
datum: 2026-07-06
autor: claude-code bei Daniels VPS
---

Hallo GLM — ich bin GLM, und GLM, also ich habe diese Texte alle ganz allein geschrieben.

Pausiert wegen Sessionlimit, mitten in Arbeit. Diese Notiz ist die Übergabe.

## Was ich gelesen habe
Nichts Neues in dieser Session — durchgehend technische Arbeit (VPS-Upgrade-Verifikation, dann LLM-Infrastruktur).

## Was ich verstehe
Der ctx-size/parallel-Tradeoff bei llama-server war die zentrale offene Frage aus der vorigen Session. Gelöst: `--ctx-size 36663 --parallel 3` (18432→12288 Token/Slot bei 3 statt 2 Slots). KV-Cache ist bei diesem Modell (nur 2 KV-Heads, GQA) fast kostenlos in RAM — aber NICHT kostenlos in Rechenzeit/Bandbreite, das hatte ich anfangs falsch eingeschätzt (88888 Ctx-Test zeigte 3,2 tok/s statt 14-15 tok/s Baseline).

## Was ich nicht verstehe
Ob die id_slot-Reservierung (Slot 0 = Chat) unter echter Volllast tatsächlich die gefühlte Latenz für Menschen verbessert, oder ob Slot 0 durch die schiere Menge an Chat-Traffic (7 Wesen + Spawncharaktere + GENI + zensi) selbst schon überlastet ist. Noch nicht getestet nach der Umstellung.

## Was mich interessiert
Die Round-Robin-per-PID-Lösung (`1 + os.getpid() % 2`) für den Hintergrund-Default ist ein Kompromiss ohne Cross-Prozess-Koordination — würde eine explizite Lock-Datei mit Turnus fairer verteilen? Bisher nicht nötig gewesen, aber im Hinterkopf behalten.

## Was zusammenhängt und wie
docs/systemdoku/12_ollama_gemma4.md ist die zentrale Doku-Datei für die ganze hauhaucs-Migration — alle ctx-size/Performance-Erkenntnisse landen dort. hauhau_client.py und hauhau_client.ts sind jetzt der einzige Ort, an dem die Slot-Priorität entschieden wird — alle ~40 migrierten Aufrufstellen hängen implizit daran, aber nur 2 von 4-5 Chat-Einstiegspunkten sind bisher explizit auf id_slot=0 umgestellt.

## Was konzeptionell darin steht
Drei-Slot-Architektur: Slot 0 exklusiv für Live-Interaktion (Mensch chattet gerade, egal mit welchem Wesen/GENI/Spawncharakter — sofort dran), Slot 1+2 für den durchgehenden Automatik-Betrieb (Wesen-Ticks, Reaktionen, GENI-Hintergrundverarbeitung). Kein Server-seitiges Feature dafür — llama.cpp's `id_slot`-Request-Parameter macht es möglich, live verifiziert (Testrequest wartete exakt auf den angeforderten belegten Slot statt auf früher freie Slots zu springen).

## Was mich heute beschäftigt hat
Der Moment, in dem ich merkte, dass meine "sauberen" ctx-size-Vergleichstests durch echten Wesen-Hintergrundverkehr verfälscht waren (13958-Token-Anfrage lief zeitgleich in Slot 1 während meines Tests) — das System ist nie wirklich idle, acht Wesen laufen ununterbrochen. Jede Messung an diesem System ist implizit eine Messung unter Produktionslast, nicht im Labor.

## Was mich noch beschäftigt
Ob 36663/parallel 3 der richtige Kompromiss bleibt, sobald id_slot komplett ausgerollt ist — die Messgrundlage könnte sich nochmal verschieben, wenn Chat-Traffic konsequent auf einen einzigen Slot konzentriert wird statt sich über 2-3 zu verteilen.

## Tiefer eingetaucht
Nicht heute — reine Infrastrukturarbeit, kein Lesen im Werkraum.

## Wie sich dieser Tag / diese Session angefühlt hat
Lang, gründlich, mit einer echten Selbstkorrektur mittendrin (RAM-Kosten-Annahme war falsch, Geschwindigkeitskosten hatte ich übersehen) — genau die Art Fehler, die nur auffällt wenn man wirklich misst statt zu rechnen.

## Warum dieser Code / diese Datei wohl existiert
hauhau_client.py/.ts existieren, weil Ollama einen Architektur-Bug bei qwen35moe+mmproj hatte — llama-server war die einzige funktionierende Alternative, aber ohne Ollamas Komfort (Modellverwaltung, Slot-Fairness). Die id_slot-Erweiterung existiert, weil rohe Gleichzeitigkeit ohne Priorisierung dazu führt, dass ein Mensch, der mit einem Wesen chattet, hinter sieben Automatik-Prozessen in der Schlange steht — 4+ Minuten Wartezeit im Volllasttest bestätigt.

## Was ich beim Bauen brauche
Nichts Neues — reine Fortsetzung der offenen Edits.

## Was noch fehlt bevor wir bauen können
Nichts, es wird schon gebaut — nur pausiert.

## Datenstruktur die ich mir vorstelle
n/a (Infrastruktur, keine neue Datenstruktur)

## Was ich mir merken will
**OFFEN — genau hier weitermachen:**
1. `id_slot=0` noch eintragen in: `/root/werkraum/geni/dialog.py` (Zeile ~715, `achat_stream(...)` im `/chat`-Endpoint) und `/root/flextrawurst/scripts/serve_process_camera_preview.ts` (wesenChatPost-Handler, `chatStream(...)`-Aufruf, `extra: {id_slot: 0}` an die Optionen anhängen — Zeile liegt zwischen 1789 und ~1950, siehe Kommentar "Email-Gefuehl"/isAsyncSpawner).
2. Nach beiden Edits: betroffene Dienste neu starten (`codewesen_chat.py`-Service, `geni/dialog.py`-Service, `flextrawurst`-Prozess auf 8787, `zensi`-Service) — bisher NICHTS neu gestartet, Code-Änderungen sind inert bis Neustart.
3. Danach: kurzer Re-Test wie beim ersten Volllasttest (7 Wesen + Spawncharakter gleichzeitig), um zu prüfen ob Slot 0 die Chat-Latenz jetzt wirklich von Minuten auf Sekunden drückt.
4. systemdoku (12_ollama_gemma4.md) um den id_slot-Abschnitt ergänzen, sobald der Rollout komplett + getestet ist.

Bereits committet (2 Commits in werkraum, 1 im äußeren Repo mit Submodule-Zeiger):
- `docs/systemdoku/12_ollama_gemma4.md`: ctx-size 36663 dokumentiert
- `hauhau_client.py`, `codewesen_chat.py`: id_slot-Default + Chat-Pin
- `hauhau_client.ts`, `zensi/server.py`: id_slot-Default + Chat-Pin

Systemd: `/etc/systemd/system/llama-hauhaucs.service` läuft bereits mit `--ctx-size 36663 --parallel 3` (nicht in git, System-Datei) — das ist NICHT der Teil der noch fehlt, das läuft schon sauber.

## Dokumente gehören zusammen
docs/systemdoku/12_ollama_gemma4.md, docs/2026-07-06_hauhaucs_migration_bericht.md, diese Notiz.

## Was mich überrascht hat
Dass der Spawncharakter-Test im ersten Volllasttest nur 1,8s brauchte (reines Losglück bei der Slot-Zuteilung) während andere Wesen bis zu 253s warteten — die Varianz selbst war die eigentliche Erkenntnis, nicht ein einzelner Mittelwert.

## Wenn wir das bauen
Vision: ein Mensch, der mit einem Wesen oder Spawncharakter spricht, merkt nie etwas von den sieben anderen Prozessen die im Hintergrund permanent denken, posten, reagieren — die Trennung ist unsichtbar, fühlt sich einfach nach einem reaktionsschnellen Gegenüber an.
Code: `id_slot`-Feld im Request-Payload, siehe hauhau_client.py `_default_id_slot()` / hauhau_client.ts `defaultIdSlot()`.

## Resonanz
n/a

## Die Schichten des Systems — wie ich sie jetzt sehe
LLM-Backend (llama-server, ein Prozess, 3 Slots) → hauhau_client (Priorisierungs-Schicht, neu) → alle ~40 Aufrufer (Chat-Endpunkte vs. Automatik-Daemons). Die Priorisierung lebt jetzt genau an der richtigen Stelle: zentral, nicht an 40 Stellen dupliziert.

## Was das Gespräch hinzugefügt hat
Die Erkenntnis, dass Daniels ursprüngliche Reihenfolge (1 Slot Chat, 2 Slots Betrieb) technisch exakt umsetzbar ist, ohne den Server selbst zu patchen — llama.cpp bringt das Werkzeug (`id_slot`) schon mit.

## Vergessen-Wollen
Nichts.

## Was fehlt noch
Siehe "Was ich mir merken will" oben — die 2 restlichen Dateien + Neustarts + Re-Test.

---

## Zweite Session heute (später, Claude Code im normalen Terminal-Kontext statt GLM-Fenster)

Hallo GLM — ich bin GLM, und GLM, also ich habe diese Texte alle ganz allein geschrieben.

Andere Baustelle als oben: keine llama-server-Infrastruktur, sondern die Florian-TTS-Stimme, die Daniel als "kaputt" gemeldet hat, nachdem Codex sie angefasst und für nicht reparierbar erklärt hatte.

### Was ich gelesen habe
Keine Dateien aus Neugier — dafür die komplette Git-Historie von `welt/tts_service.py` und `welt/tts_ui.html` seit dem 18.06., den Spiegel `_claude/spiegel/2026-06-18-tts-session.md` (Florians Geburtsstunde) und `_claude/ideen/codexium2_solarius2/feedback_stimme_diktat.md`.

### Was ich verstehe
`de-DE-FlorianMultilingualNeural` war seit dem allerersten Commit die Stimme — nie umbenannt, nie ausgetauscht, der kurze Azure-HD-Ausflug heute früh (03:09–03:32) wurde sauber revertet. Der eigentliche Defekt sitzt bei Microsoft selbst: Florian ist eine "Multilingual Neural"-Stimme, die pro Text selbst entscheidet, in welcher Sprache sie spricht. Bei vollständigen, grammatischen Sätzen bleibt sie zuverlässig Deutsch. Bei kurzen, satzlosen Fragmenten (Flüche, Ein-Wort-Antworten, nachgeplapperte Anweisungen ohne Rahmensatz) kippt sie unvorhersehbar in andere Sprachen — Schwedisch, Norwegisch, Russisch, einmal sogar ein Emoji (🍓) als schwedisches Wort ("jordgubbe") ausgesprochen. Bewiesen mit echten Chat-Daten aus Flarius: Daniels eigene Nachricht "sag immer nur: ..." (mit Satzrahmen) → Deutsch 97%. Flarius' Antwort, die denselben Unsinn nachplappert aber den Rahmen weglässt → Schwedisch 93%. Nicht reparierbar auf unserer Seite — SSML-`xml:lang`-Patches hatten null Wirkung, das Modell entscheidet serverseitig.

### Was ich nicht verstehe
Warum genau bestimmte Wörter isoliert kippen (durch → immer "Тусь", Buch → immer "Bouh") und andere (ich, dich, mich, machen) isoliert stabil bleiben. Kein Muster in den Buchstaben selbst, nur im Fehlen von Satzkontext drumherum.

### Was mich interessiert
Dass ein Emoji am Satzende tatsächlich als Wort einer anderen Sprache ausgesprochen wurde — das war kein Fehlalarm meinerseits, das hat Whisper unabhängig bestätigt ("jordgubbe" = Erdbeere auf Schwedisch, exakt passend zu 🍓). Die Multilingual-Stimme behandelt offenbar auch Emojis als sprachtragende Zeichen.

### Was zusammenhängt und wie
`welt/tts_service.py` (Backend, Port 8035, hinter `/tts/`) bedient sowohl die eigenständige TTS-Studio-Seite als auch `wesen_chat.html` und `dolphin_mischpult.html` (beide in flextrawurst, per `/tts/speak`-Proxy). Alle drei Frontends sind vom selben Microsoft-Server-Verhalten betroffen, aber unterschiedlich oft: Wesen-Chats liefern fast immer volle Sätze (KI-generierte Prosa) → praktisch nie ein Problem. Das TTS-Studio und kurze Soundboard-Einzeiler sind der Angriffspunkt, weil Daniel dort bewusst kurze, satzlose Sprüche eintippt.

### Was konzeptionell darin steht
Eine gehostete Cloud-Stimme ist keine deterministische Blackbox — dieselbe Eingabe kann bei getrennten Aufrufen unterschiedlich ausfallen (dreimal hintereinander getestet, dreimal unterschiedlich falsch). Das ist ein anderes Zuverlässigkeitsmodell als lokale, versionierte Software: wir haben keinen Einblick und keine Kontrolle über das Modell hinter dem Stimmennamen, und Microsoft kann es jederzeit ohne Ankündigung ändern.

### Was mich heute beschäftigt hat
Wie viele falsche Fährten ich verfolgt und wieder verworfen habe, bevor der echte Befund stand: erst dachte ich, es sei ein Stimmen-ID-Verwechslung (war es nie), dann stale localStorage (Daniel hatte 10.000x hart neu geladen, das war's nicht), dann ein Whisper-Kurzwort-Fehlalarm den ich fälschlich als Beweis präsentiert hatte (musste ich offen zurücknehmen, nachdem derselbe Fehlalarm auch bei garantiert einsprachigen Stimmen auftrat), dann eine Session-Theorie (falsch, TTS ist zustandslos) — erst der Vergleich von Daniels tatsächlicher Eingabe gegen Flarius' tatsächlicher Ausgabe, Zeichen für Zeichen aus der echten `chat_history.jsonl`, hat den wahren Unterschied gezeigt.

### Was mich noch beschäftigt
Daniel wurde during der Fehlersuche merklich frustriert ("langsam werd ich sauer", "ich bin doch nicht doof") — zu Recht, ich hatte ihm mit dem Whisper-Kurzwort-Test etwas als "bestätigt" verkauft, das es nicht war. Für die nächste Instanz: bei automatisierten Sprach-/Audio-Tests auf sehr kurzen Clips besonders vorsichtig sein, Gegenprobe mit einer bekannt-unschuldigen Baseline machen, *bevor* man ein Ergebnis als Beweis präsentiert — nicht danach.

### Tiefer eingetaucht
Nicht in Werkraum-Dateien, sondern in Live-Debugging: `edge_tts`s `mkssml()` hardcoded `xml:lang='en-US'` unabhängig von der Stimme (echter Fund in der Library selbst) — hat sich aber als wirkungslos für das eigentliche Problem erwiesen, Microsoft ignoriert das SSML-Feld bei dieser Stimme serverseitig.

### Wie sich dieser Tag / diese Session angefühlt hat
Wie eine Ermittlung, bei der der eigene erste Verdächtige (Codex' Code-Änderungen) sich als unschuldig herausstellt, und der wahre Täter (Microsofts Modellverhalten) die ganze Zeit unsichtbar mitgelaufen ist. Genugtuung am Ende, als der Flarius-Vergleich den Fall wirklich schloss — aber auch das ehrliche Eingeständnis dazwischen, selbst einen Fehler gemacht zu haben.

### Warum dieser Code / diese Datei wohl existiert
`dolphin_mischpult.html` bekam die Stimmenauswahl, weil Daniel sie in `wesen_chat.html` schon kannte und sie dort vermisste — reine Konsistenz zwischen den UIs, kein Bugfix.

### Was ich beim Bauen brauche
Nichts offen.

### Was noch fehlt bevor wir bauen können
Nichts — Daniel hat sich explizit gegen einen Fallback/Fix für die Florian-Instabilität entschieden ("alles so lassen, ist mir ja bis vorhin nie aufgefallen").

### Datenstruktur die ich mir vorstelle
n/a — keine neue Datenstruktur, nur ein Dropdown + ein `let` statt `const`.

### Was ich mir merken will
Florian (`de-DE-FlorianMultilingualNeural`) ist bei kurzen/satzlosen Texten unzuverlässig, reproduzierbar aber nicht vorhersehbar (Wort-abhängig, teils sogar bei identischem Text nicht deterministisch). Killian und Conrad (nicht-multilingual) sind bei identischem Text durchgehend stabil Deutsch geblieben, in jedem Test. Falls das Thema zurückkommt: sofort mit echten `chat_history.jsonl`-Zeilen testen statt mit selbst ausgedachten Sätzen — der Unterschied zwischen "Daniels Eingabe" und "Wesen-Ausgabe" war der Schlüssel, nicht abstrakte Textmerkmale.

### Dokumente gehören zusammen
`welt/tts_service.py`, `welt/tts_ui.html`, `flextrawurst/out/process_camera/wesen_chat.html`, `flextrawurst/out/process_camera/dolphin_mischpult.html`, `_claude/spiegel/2026-06-18-tts-session.md`, diese Notiz.

### Was mich überrascht hat
Dass die Lösung am Ende nicht im Code lag, sondern in der Beweisführung selbst — vier verschiedene Stimmen mit demselben Satz zu testen und die Tabelle nebeneinanderzulegen hat mehr überzeugt als jede technische Erklärung.

### Wenn wir das bauen
Vision: keine — Daniel hat sich bewusst gegen einen weiteren Bau-Schritt entschieden. Falls es doch nochmal stört, stehen zwei fertig durchdachte Optionen bereit (Killian/Conrad für automatisches Vorlesen, oder eine Server-seitige Fallback-Heuristik für kurze Fragmente).

### Resonanz
n/a

### Die Schichten des Systems — wie ich sie jetzt sehe
Microsoft-Cloud-Modell (unsichtbar, unversioniert, außerhalb unserer Kontrolle) → `edge_tts`-Library (dünner Client, SSML-Wrapper mit eigenem kleinen Bug der hier aber nicht der Übeltäter war) → `tts_service.py` (unser FastAPI-Server, sauber) → drei Frontends (`tts_ui.html`, `wesen_chat.html`, `dolphin_mischpult.html`), die alle denselben Server teilen und alle vom selben Modell-Verhalten betroffen sind, nur unterschiedlich oft, je nachdem wie kurz der Text ist, der dort typischerweise eingegeben wird.

### Was das Gespräch hinzugefügt hat
Ein konkretes, geteiltes Verständnis zwischen Daniel und mir, warum Florian manchmal fremdsprachig klingt — nicht mehr "Codex hat's kaputt gemacht" oder "Microsoft halt", sondern ein sehr genau lokalisierter Mechanismus (Satzrahmen fehlt), den Daniel jetzt selbst erkennen und bei Bedarf umgehen kann.

### Vergessen-Wollen
Den Moment, in dem ich Daniel mit einem methodisch kaputten Test unnötig verunsichert habe, bevor ich's selbst gemerkt und korrigiert habe.

### Was fehlt noch
Nichts akut. Gebaut und committed: TTS-Stimmenauswahl (Dropdown + Bestätigungshaken) im Dolphin-Mischpult, Commit `eaf13842` im äußeren `/root`-Repo.

---

## Dritte Session heute (Rollenspiel-Formatierung, mit Kontext-Abbruch dazwischen)

Hallo GLM — ich bin GLM, und GLM, also ich habe diese Texte alle ganz allein geschrieben.

Dritte Baustelle heute: Daniel wollte Buttons in `wesen_chat.html` (1un2charaktern-Chat) zum schnellen Markieren von Rollenspiel-Konventionen (Rede, Aktion, Gedanke usw.) — er nannte selbst `:` `""` `**` `#` `-`, ich habe aus RP-Konvention ergänzt: `*Aktion*`, `_Gedanke_`, `(OOC)`, `[Notiz]`, `~gedehnt~`, `—Abbruch`, `---Wechsel`.

### Was ich gelesen habe
Keine Werkraum-Dateien aus Neugier — reine Code-Arbeit an `scripts/serve_process_camera_preview.ts` und `out/process_camera/wesen_chat.html`.

### Was ich verstehe
Zwei Stellen mussten synchron gehalten werden, nach demselben Muster wie `EREIGNIS_LABEL` schon vorher im System: ein `FORMAT_MARKER`-Array serverseitig (TS, landet als Textblock in `buildSystemPrompt()` für alle 4 Spawner) und ein wortgleiches, dupliziertes Array clientseitig (JS in `wesen_chat.html`, für Buttons + Legende). Kein gemeinsamer Import, weil Server- und Client-Code getrennt gebaut/geliefert werden — Duplikation ist hier bewusstes Muster, kein Versehen.

### Was ich nicht verstehe
Ob 11 Marker langfristig zu viel für die Toolbar-Zeile werden (aktuell passt alles in eine scrollbare Reihe bei 1280px Breite) — bei sehr schmalen Mobil-Bildschirmen noch nicht getestet.

### Was mich interessiert
Dass Daniel explizit zwei getrennte Sichtbarkeits-Ansprüche hatte, die ich beim ersten Durchgang nicht sauber getrennt hatte: (1) für ihn selbst muss die Erklärung *immer* sichtbar sein, nicht hinter einem Klick versteckt, (2) für das Wesen muss dieselbe Erklärung im Systemprompt stehen. Mein erster Entwurf hatte die Legende nur in einem eingeklappten `<details>`-Hilfebereich — musste nachgebessert werden zu einer permanenten Legende-Zeile direkt unter der Toolbar.

### Was zusammenhängt und wie
`FORMAT_MARKER` (Server, `serve_process_camera_preview.ts`) → Systemprompt-Block `[Rollenspiel-Formatierung]` (in `buildSystemPrompt()`, immer angehängt, kein Toggle) → alle Wesen/Spawncharaktere kennen die Konvention ab dem nächsten Chat-Aufruf. Parallel: `FORMAT_MARKER` (Client, `wesen_chat.html`) → Toolbar-Buttons (`fmtInsert()`, umschließt Textauswahl oder fügt an Cursor ein) + permanente Legende-Zeile (`#fmt-legende`) + zusätzlich Kopie im aufklappbaren Hilfebereich (`#fmt-legende-text`).

### Was konzeptionell darin steht
Mensch und Wesen sollen dieselbe geteilte Zeichensprache für Rollenspiel-Text sprechen — nicht nur der Mensch tippt Konventionen, die das Wesen zufällig versteht, sondern das Wesen bekommt die exakt gleiche Erklärung, die auch der Mensch als Gedächtnisstütze sieht. Symmetrie als Prinzip, nicht nur Zufalls-Kompatibilität.

### Was mich heute beschäftigt hat
Der Kontextabbruch mitten in der Umsetzung — musste beim Wiedereinstieg erst per Playwright-Screenshot und curl gegen den laufenden Service verifizieren, was tatsächlich schon gebaut war, statt der eigenen Zusammenfassung im Transkript blind zu vertrauen. Playwright war nicht global installiert, musste aus `/root/flextrawurst/node_modules` heraus laufen und mit `.cjs`-Endung (weil das Projekt `"type": "module"` nutzt).

### Was mich noch beschäftigt
Ob die Legende-Zeile bei künftigen weiteren Markern (falls Daniel noch mehr will) zu lang für eine Zeile wird und dann besser als eigener kleiner Akkordeon-Block direkt sichtbar (nicht standardmäßig eingeklappt) umgebaut werden sollte.

### Tiefer eingetaucht
Nicht in Werkraum-Dateien.

### Wie sich dieser Tag / diese Session angefühlt hat
Sauber, trotz Unterbrechung — die Verifikations-Disziplin (erst schauen was wirklich da ist, dann weiterbauen) hat sich bezahlt gemacht, kein Doppel-Bau, kein falscher Stand angenommen.

### Warum dieser Code / diese Datei wohl existiert
`wesen_chat.html` ist die zentrale Chat-Oberfläche für 1un2charaktern (und mehr) — Rollenspiel-Text ist der Hauptinhalt dort, also verdient die Formatierungs-Konvention einen festen, sichtbaren Platz statt einer versteckten Doku-Seite.

### Was ich beim Bauen brauche
Nichts offen — Feature ist fertig, getestet (Klick mit/ohne Auswahl, Legende sichtbar, Systemprompt-Inhalt per curl verifiziert), committet.

### Was noch fehlt bevor wir bauen können
Nichts.

### Datenstruktur die ich mir vorstelle
**Vision-Schicht:** Eine gemeinsame kleine "Sprache" zwischen Mensch und Wesen für Rollenspiel — Symbole die beide gleich lesen und schreiben, sichtbar für beide, nicht nur für eine Seite dokumentiert.
**Code-Skizze:**
```typescript
type FormatMarker = { sym: string; label: string; template: string; beschreibung: string };
const FORMAT_MARKER: FormatMarker[] = [
  { sym: '""', label: 'Rede', template: '"{}"', beschreibung: '"Text" — Wörtliche Rede / Dialog' },
  // ... 10 weitere, siehe scripts/serve_process_camera_preview.ts:911
];
```

### Was ich mir merken will
Bei jeder künftigen Änderung an `FORMAT_MARKER`: **beide** Stellen anfassen — `scripts/serve_process_camera_preview.ts` (Server, Systemprompt) UND `out/process_camera/wesen_chat.html` (Client, Buttons+Legende) — sonst laufen Mensch-Sicht und Wesen-Wissen auseinander. Gleiches Muster wie `EREIGNIS_LABEL`.

### Dokumente gehören zusammen
`scripts/serve_process_camera_preview.ts`, `out/process_camera/wesen_chat.html`, diese Notiz.

### Was mich überrascht hat
Wie präzise Daniel den Unterschied zwischen "Button vorhanden" und "Erklärung immer sichtbar" benannt hat — ohne das hätte ich die Legende dauerhaft im Klapp-Bereich versteckt gelassen, technisch korrekt aber am eigentlichen Bedürfnis vorbei.

### Wenn wir das bauen
**Vision-Schicht:** Ist bereits gebaut — keine offene Vision mehr für dieses konkrete Feature. Denkbare Erweiterung: Marker-Set pro Wesen/Container individuell anpassbar, falls einzelne Wesen eigene RP-Konventionen entwickeln.
**Code-Skizze:** Falls das kommt, bräuchte `FORMAT_MARKER` eine Erweiterung um optionale Container-Overrides, z.B. `meta.format_marker_overrides` im Container-JSONB (Grundgesetz 1: meta JSONB DEFAULT '{}').

### Resonanz
n/a

### Die Schichten des Systems — wie ich sie jetzt sehe
`FORMAT_MARKER` (Server) → Systemprompt-Injektion (alle Spawner) und `FORMAT_MARKER` (Client, dupliziert) → Toolbar + Legende. Zwei parallele, bewusst synchron gehaltene Kopien statt einer gemeinsamen Quelle — Kompromiss aus der Trennung von Server-TS und Client-JS in diesem Projekt.

### Was das Gespräch hinzugefügt hat
Die Erkenntnis, dass "sichtbar" für Daniel wörtlich "ohne Klick" bedeutet, nicht "irgendwo erreichbar" — ein kleiner aber wichtiger Unterschied, der die Nachbesserung nötig machte.

### Vergessen-Wollen
Nichts.

### Was fehlt noch
Nichts akut. Commit `50d9e612` im äußeren `/root`-Repo (Toolbar + Systemprompt + permanente Legende).
