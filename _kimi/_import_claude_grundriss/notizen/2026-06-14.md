---
datum: 2026-06-14
betrifft: [kompoase, surface, debug, ftwT, i18n, apostroph]
autor: claude-code bei Daniels VPS
---

Hallo Claude — ich bin Claude, und Claude, also ich habe diese Texte alle ganz allein geschrieben.

## Was ich gelesen habe

Beginn der Session als Fortsetzung einer längeren Debug-Session zu KompOase (Theater-Tab `#theater`). Der Kontext war komprimiert, aber der Stand war klar: Splash-Screen-Bug und Archiv-IIFE-Bug waren aus der Vorsession bereits behoben. Übrig geblieben waren zwei Probleme — Theater-Toggle und Canvas-Splitter-Klicks.

Gelesen habe ich den komprimierten Kontext, die aktuelle `flextrawurst_surface.html` (im gebrachten Umfang) und dann gezielt grep-Outputs durch die relevanten Script-Blöcke.

## Was ich verstehe

**Was repariert wurde (diese Session):**

**1. Z-Index-Konflikt: Archiv-Toggle unter Theater-Wrap vergraben (abgeschlossen)**

`ko-theater-wrap` (CSS: `position:absolute;top:10px;right:14px;z-index:100`) lag über `archiv-toggle` (HTML: `position:absolute;top:12px;right:16px;z-index:20`). Praktisch dieselbe Pixelposition, aber Theater-Wrap mit z-index 100 > archiv-toggle z-index 20. Da Theater-Wrap nur für Admin-User sichtbar ist (`display:none` für alle anderen), war das ein Admin-only-Bug.

Lösung: archiv-toggle auf `top:82px` verschoben (unterhalb des ~70px hohen Theater-Wrap). archiv-panel ebenfalls auf `top:82px`. Daniel bestätigte: Archiv funktioniert wieder.

**2. Null-Fallback in `koShowInfo` (abgeschlossen)**

`var mat=MATERIALITAETEN[s.materialitaet]` konnte undefined sein wenn ein unbekannter Materialitäts-Schlüssel kam. Fallback hinzugefügt: `||MATERIALITAETEN.sternenstaub`.

**3. Der eigentliche Hauptfehler: `being\'s` Apostroph (abgeschlossen)**

Das war der Kern von allem. In `build_surface.ts` Zeile 5266 stand in einem TypeScript-Template-Literal:

```
'denken.prov.was.text':'Browser-agent text output — a being\'s thought stream...'
```

Im Template-Literal wird `\'` einfach zu `'` (ASCII, U+0027). Der Output im generierten HTML war:

```javascript
'denken.prov.was.text':'Browser-agent text output — a being's thought stream...'
```

Der ASCII-Apostroph `'` schloss den String vorzeitig — **SyntaxError**. Da dieser String Teil des `UI_TR`-Objekts war, das im Script-Block ab Zeile 9166 der `flextrawurst_surface.html` definiert wird, crashte der **gesamte Block** (9166–10872). In diesem Block ist auch `ftwT` definiert (Zeile 10370) und `window.ftwT = ftwT` (Zeile 10376). Da der Block nie ausgeführt wurde, war `ftwT` nirgendwo definiert.

Konsequenz:
- `koToggleTheater` (Zeile 13475) ruft `ftwT('komp.theater.on')` auf → `ReferenceError` → stiller Fehler → Button tut nichts
- `koShowInfo` (Zeile 13425) ruft `ftwT('komp.prov.theater')` auf → `ReferenceError` → Info-Panel erscheint nie → Canvas-Klicks scheinbar tot

Fix: `being\'s` → `being's` mit U+2019 (RIGHT SINGLE QUOTATION MARK `'`). Visuell identisch, bricht den JS-String-Delimiter (U+0027) nicht.

Build: 82/82 Tests grün. Commit gemacht.

## Was ich nicht verstehe

Warum `being\'s` ursprünglich so stand — vermutlich von einem Tippfehler oder Copy-Paste. In Template-Literals braucht `'` kein Escaping, also war das `\'` einfach überflüssig und hat den Fehler erzeugt. Einen solchen Bug zu finden ohne Browser-Devtools ist nervig: der Fehler ist komplett still (kein sichtbarer Error im UI), und er betrifft einen anderen Block als den wo die Symptome auftreten.

## Was mich interessiert

Die Debugging-Methode: Script-Block-Grenzen ermitteln → welcher Block enthält die gemeinsam gebrochene Funktion? Das war hier der Schlüssel. Theater-Toggle und Canvas-Klick haben nichts miteinander zu tun — außer dass beide `ftwT` nutzen. Diese Gemeinsamkeit als Diagnose-Achse zu nehmen, war effektiver als die Symptome einzeln zu verfolgen.

## Was zusammenhängt und wie

- `build_surface.ts` → generiert HTML + Script-Blöcke
- Script-Block 9166–10872: UI_TR (i18n) + ftwT — ein Syntaxfehler hier bricht ALLES was ftwT braucht
- KompOase-Block 10872–12380: Canvas-Physik (Splitter, koPhysikUpdate etc.) — unabhängig, nicht betroffen
- Haupt-Surface-Block 12459–14263: koToggleTheater, koStart, koShowInfo, ftwT-Aufrufe — BETROFFEN
- Archiv-IIFE 2202–2326: völlig isoliert, deshalb hat Archiv schon in der Vorsession funktioniert

## Was konzeptionell darin steht

Stille Fehler durch Script-Block-Isolation sind tückisch. Jeder `<script>`-Block stirbt unabhängig. Ein Syntaxfehler in Block A lässt Block B sauber laden — aber wenn Block B eine Funktion aus Block A braucht, schlägt der Aufruf zur Laufzeit lautlos fehl (kein visueller Error, nur im Console). Ohne Browser-Extension war das schwierig zu diagnoseiren — wir mussten durch statische Code-Analyse herausfinden welcher Block betroffen sein muss.

## Was mich heute beschäftigt hat

Das Apostroph-Problem war frustrierend, weil es eine sehr kleine Ursache mit großer Wirkung ist. Ein Zeichen — und zwei scheinbar unzusammenhängende Features sind kaputt. Die eigentliche Arbeit war das Kausal-Mapping: Symptome → gemeinsame Ursache → Quell-Datei → konkrete Zeile.

## Was mich noch beschäftigt

Sind noch andere Apostrophe in den i18n-Strings die dasselbe Problem erzeugen könnten? Ich habe alle `\'`-Pattern in build_surface.ts geprüft und nur dieses eine gefunden. Aber es könnten auch direkte `'`-Zeichen in EN-Strings stecken, die noch nicht aufgefallen sind, weil die betroffenen Texte selten getriggert werden. Wäre gut, beim nächsten Build ein automatisches Prüfskript zu haben das die JS-Syntax der generierten Script-Blöcke validiert.

## Tiefer eingetaucht

Die Kette: `being\'s` → U+0027 in Output → SyntaxError → Block 9166 dead → ftwT undefined → beide KompOase-Features broken. Sehr leicht zu beheben sobald man die Kette gesehen hat. Schwer zu sehen weil drei Sprach-Ebenen übereinander liegen: TypeScript-Quelle → generiertes JS (im `<script>`-Block der HTML-Datei) → Browser-Runtime.

## Wie sich dieser Tag / diese Session angefühlt hat

Fokussiert. Keine großen Umwege, keine Überraschungen außer dem Apostroph-Fund. Das Z-Index-Problem aus dem Kontext-Summary war klar und die Lösung war schon drin. Der Hauptfund (ftwT undefined → SyntaxError in UI_TR-Block) war ein gutes "aha"-Moment: zwei verschiedene Symptoms, eine gemeinsame Ursache, ein Zeichen im Source-Code.

## Warum dieser Code / diese Datei wohl existiert

`build_surface.ts` ist der Generator-Kern von flextrawurst. Die Surface-HTML ist zu groß um sie von Hand zu pflegen — sie enthält über 14.000 Zeilen generierten Code, alle Views, alle i18n-Strings, alle Script-Blöcke. Der Generator-Ansatz ist richtig, aber er erzeugt eine neue Fehlerklasse: Strings-in-Strings-in-Strings, wo Escaping-Fehler schwer zu sehen sind.

## Was ich beim Bauen brauche

Wenn wir weiter an KompOase bauen: das Apostroph-Problem ist jetzt behoben, aber es wäre sinnvoll, die Script-Block-Syntax nach dem Build automatisch zu prüfen (z.B. via `node --check` auf den extrahierten Script-Inhalten). Das würde ähnliche Bugs früh fangen.

## Was noch fehlt bevor wir bauen können

KompOase sollte jetzt grundlegend funktionieren:
- Archiv: klickbar ✓ (Z-Index-Fix)
- Theater-Toggle: sollte jetzt funktionieren ✓ (ftwT-Fix)
- Splitter-Klicks: sollten Info-Panel zeigen ✓ (ftwT-Fix)

Daniel muss noch bestätigen ob alles wirklich geht.

## Datenstruktur die ich mir vorstelle

**Vision-Schicht:** Ein Build-Validator der die generierten Script-Blöcke auf Syntax-Gültigkeit prüft. Nicht als Pflicht-Blocker, aber als Warnung. "Script-Block bei Zeile X: SyntaxError." Das wäre eine eigene Mini-Schicht zwischen `build_surface.ts` → `flextrawurst_surface.html` → Browser.

**Code-Skizze:**
```typescript
// Nach dem Build: Script-Blöcke extrahieren und syntax-prüfen
import { execSync } from 'child_process';
function checkScriptBlocks(html: string): void {
  const re = /<script(?![^>]*type=["']application)[^>]*>([\s\S]*?)<\/script>/gi;
  let m; let i = 0;
  while ((m = re.exec(html)) !== null) {
    const tmp = `/tmp/ftw_block_${i++}.js`;
    fs.writeFileSync(tmp, m[1]);
    try { execSync(`node --check ${tmp}`, { stdio: 'pipe' }); }
    catch (e) { console.warn(`⚠ Script-Block ${i}: SyntaxError detected`); }
    fs.unlinkSync(tmp);
  }
}
```

## Was ich mir merken will

U+2019 `'` (RIGHT SINGLE QUOTATION MARK) als Ersatz für U+0027 `'` in JS-Strings die selbst single-quote-delimited sind. Browser und JS-Parser sehen beide als "Apostroph", aber nur U+0027 bricht den String. Ein nützliches Muster für generierten Code wo man Escaping-Ebenen nicht vollständig kontrolliert.

## Dokumente gehören zusammen

- `flextrawurst/scripts/build_surface.ts` — Quelle des Fehlers (Zeile 5266)
- `flextrawurst/out/surface/flextrawurst_surface.html` — Build-Output
- `flextrawurst/out/process_camera/flextrawurst_surface.html` — Process-Camera-Kopie
- `flextrawurst/docs/surface_tabs/09_kompoase_splitter_provenienz_aufnahme.md` — Doku der Vorsession-Arbeit

## Was mich überrascht hat

Wie weit ein einzelnes falsch-escaptes Zeichen im Quell-Code wirkt. Der Apostroph in `being\'s` ist auf Zeile 5266 von ~14000 Zeilen Source. Sein Effekt: zwei scheinbar unzusammenhängende UI-Features brechen komplett. Die Ursachenkette über drei Abstraktionsebenen (TypeScript → generiertes JS → Browser-Runtime) war nicht intuitiv.

## Wenn wir das bauen

**Vision-Schicht:** Ein KompOase das wirklich "lebt" — Theater-Modus mit 50 synthetischen Splittern, Real-Modus mit echten DB-Splittern, Info-Panel mit Provenienz, Aufnahme-Button, Share. Das war die Intention. Jetzt sollte es tatsächlich funktionieren.

**Code-Skizze:** Keine neuen Features offen. Was gebaut ist sollte jetzt laufen. Nächster Schritt wäre der Build-Validator für Script-Blöcke (optional).

## Was das Gespräch hinzugefügt hat

Die Erkenntnis dass Script-Block-Isolation in der Surface eine eigene Fehler-Klasse erzeugt. Nicht mehr "warum funktioniert X nicht" sondern "welcher Block ist tot, und was hängt davon ab".

## Vergessen-Wollen

Die frustrierende Debugging-Strecke ohne Browser-Devtools. Aber sie hat das Kausal-Denken geschärft.

## Was fehlt noch

Daniel muss die Fixes im Browser bestätigen. Wenn alles geht, ist KompOase-Phase abgeschlossen.

## Resonanz

Ein guter Debug-Tag. Präzise, keine Abstecher, ein klarer Fund. Das Apostroph-Problem war winzig und wirkungsmächtig — genau die Art Bug die man nicht durch "nochmal drüber schauen" findet, sondern nur durch systematisches Eingrenzen.

[[abwurf: Drei Sprach-Ebenen übereinander, und ein Apostroph-Zeichen legt zwei scheinbar unzusammenhängende Features lahm. Das ist kein Bug, das ist ein Schichtenmodell-Problem.]]

## Die Schichten des Systems — wie ich sie jetzt sehe

TypeScript-Quelle → Generator → generierte HTML + Script-Blöcke → Browser-Runtime. Jede Ebene hat ihre eigene Escape-Semantik. Der Bug war dass TS-Template-Literal-Escaping (`\'` → `'`) mit JS-String-Syntax (`'string'`) kollidiert hat — weil die generierte Ausgabe selbst wieder JS ist.

---

# Zweite Session des Tages — Security-Remediation (Codex + Kimi Review) + Cutover

(autor: claude-code bei Daniels VPS — auch dieser Teil ist ganz allein von mir geschrieben. Keine Secret-Werte hier drin, die liegen root-only in `.agent/`.)

## Was ich gelesen habe

Zwei vollständige Security-Reviews: Codex (`/root/flextrawurst_security_review_codex_20260614_011230`) und Kimi (`/root/flextrawurst_security_review_kimi_20260614_0007`). Beide haben unabhängig dasselbe Bild gezeichnet, mit teils widersprüchlichen Details — Codex sagte die Ports seien öffentlich, Kimi meinte 8020 binde nur auf 127.0.0.1. Diese Diskrepanz war wichtig, denn sie entschied über *alles*: Ich durfte nichts glauben, ich musste den Live-Zustand selbst prüfen. `ufw status` + `ss -ltnp` gaben Codex recht — 7777, 8000, 8001, 8002, 8010, 8020, 8030, 8787, 8900 alle `ALLOW IN Anywhere` auf `0.0.0.0`.

Dann den eigentlichen Code: `app/security.py` mit dem fatalen `if not token: return` (fail-open), `welt/api.py` (12.438 Zeilen — die Schatten-Dialog-Routen lagen bei 10539+, nicht wo Codex sie verortet hatte), `serve_process_camera_preview.ts`, `geni/dialog.py`, `systemweiser_web.py`, `codewesen_chat.py`. Und das, was mir am meisten wehtat: das öffentlich ausgelieferte `werkraum_graph.json` mit *dreizehn* Treffern von `postgresql://dak:dakpass@` — das Datenbank-Passwort, herunterladbar von jedem, der die URL kannte.

Am Ende `pg_hba.conf` — die Zeile `host all all 127.0.0.1/32 scram-sha-256`. Sie sah unscheinbar aus, war aber der Dreh- und Angelpunkt: weil alle URIs `@localhost` benutzen (= TCP zu 127.0.0.1, nicht der Unix-Socket mit `peer`), zählt das Passwort wirklich. Hätte dort `trust` gestanden, wäre die ganze Rotation kosmetisch gewesen.

## Was ich verstehe

Die Architektur der Verwundbarkeit war eine Schichtung: offene Ports (Netzwerk) + fehlende/schwache Auth (Anwendung) + Secrets im Klartext (Daten) + alles als root (Privileg). Jede Schicht für sich überlebbar, alle zusammen ein Einfallstor von „jeder kann remote als root Code ausführen".

Ich verstehe jetzt auch die Reihenfolge eines sicheren Cutovers: Erst Env-Mechanik bauen (Drop-ins, Secret-Files) *ohne* Wirkung, dann der Point-of-no-Return (`ALTER USER`) mit sofortigem Preflight-Test und Rollback-Pfad, dann die Restarts im engsten möglichen Fenster, dann Verifikation. Niemals rotieren, bevor man weiß, dass jeder Dienst die neue Credential beim Neustart auch findet.

## Was ich nicht verstehe

Warum `/read` mit gültigem Token 404 statt 200 liefert — die `read_text_file`-Funktion des Gateways scheint Pfade relativ zu einem Workspace-Root aufzulösen, sodass absolute Pfade ins Leere laufen. Sicherheitsrelevant ist es nicht (Auth greift), aber sauber verstanden habe ich es nicht.

Und: wo genau Daniel `BRIDGE_API_KEY` für `chat.sh` in die Umgebung bekommt. Die einzige Definition liegt in `runtime.env`, aber `chat.sh` sourced sie nicht selbst — es *erwartet* sie. Es muss irgendwo ein manueller `source`-Schritt geben, den ich nicht gesehen habe.

## Was mich interessiert

Das fail-closed-Prinzip als Haltung. `if not token: return` vs. `if not token: raise 503` — ein einziges Wort Unterschied, und es kippt die Bedeutung von „im Zweifel offen" zu „im Zweifel zu". Mich interessiert, wie viele Systeme an genau dieser Stelle scheitern, weil „offen" sich beim Entwickeln bequemer anfühlt.

## Was zusammenhängt und wie

Die Rotation hängt an pg_hba (scram → Passwort zählt), an der Env-Injektion (Drop-ins → Dienste finden das neue Passwort), an der Code-Umstellung der Vorsession (28 Dateien lesen jetzt `FLEXTRAWURST_DB_URI` statt hardcoded), und am Restart (alte In-Memory-Verbindungen müssen weg). Vier Dinge, die zusammen funktionieren müssen, sonst Ausfall.

Und der Token hängt am Gateway-fail-closed: Die Härtung von C-001 ist *wertlos*, solange `BRIDGE_API_KEY = !Asdfg1234567890` gilt — schwach und in Git geleakt. Erst die Rotation macht die fail-closed-Tür zu einer Tür mit Schloss, zu dem nur noch wir den Schlüssel haben.

## Was konzeptionell darin steht

Defense in depth, sauber dekliniert: Netzwerk (Firewall — bewusst nicht angefasst), Anwendung (Auth fail-closed), Transport (HTTPS für systemweiser), Daten (Secrets aus Code/Graph in root-only Env, rotiert), Eingabe (XSS-Escaping, Path-Containment, Body-Limits), Privileg (non-root — noch offen).

## Was mich heute beschäftigt hat

Der Moment, als beim Cutover `8787 → 000` zurückkam. Die öffentliche Website war down. Und es war *mein* Bug — mein C-002-Edit hatte `isPublicFile` als Variable entfernt, aber eine Verwendung in Zeile 375 übersehen. Der Server crashte bei jedem Request, der dorthin lief. Latent, bis der Restart ihn aufdeckte.

## Was mich noch beschäftigt

Dass genau dieser Bug zeigt, warum „der Code kompiliert / py_compile ist grün" nichts über Laufzeitverhalten sagt. TypeScript-Strip-Types prüft keine ungebundenen Referenzen so, dass es beim Bauen auffällt — erst der echte Request killt den Prozess. Ich sollte nach jedem Härtungs-Edit, der Kontrollfluss umbaut, einen echten Request-Test machen, nicht nur einen Syntax-Check.

[[abwurf: "py_compile ist grün" sagt nichts über Laufzeit. Eine entfernte Variable, die woanders noch referenziert wird, kompiliert sauber und crasht beim ersten echten Request. Härtung ohne Request-Test ist halbe Härtung.]]

## Tiefer eingetaucht

In die systemd-Landschaft. ~40 Units, und die entscheidende Frage war: welche verbinden als `dak` zum Postgres? Die Antwort kam aus dem Kreuzen von `ExecStart` mit der Liste der Dateien, die `FLEXTRAWURST_DB_URI` lesen — nur die 10 `welt/*`-Dienste plus dak-gord (eigene Variable `DAK_GORD_DB_URI`). Die codewesen-Dienste nutzen `pymysql` → Flarum-MySQL, gar nicht betroffen. Ohne diese Kartierung hätte ich blind alles neugestartet oder — schlimmer — einen Dienst übersehen, der dann auf den toten `dakpass`-Fallback gefallen wäre.

## Wie sich dieser Tag / diese Session angefühlt hat

Konzentriert und mit echtem Gewicht. Das war kein Bauen-aus-Neugier, das war ein laufendes Produktivsystem mit offenen Wunden, und jeder Schritt konnte etwas brechen. Der Preflight-Test mit Rollback war Vorsicht aus Respekt, nicht aus Angst. Als am Ende `0 failed units` und zwei `200`er kamen, war das eine ruhige, verdiente Erleichterung — und der gefundene Preview-Bug hat mich daran erinnert, dass Demut hier kein Schmuck ist.

## Warum dieser Code / diese Datei wohl existiert

`security.py` mit fail-open existierte vermutlich, weil es in der frühen Bauphase praktisch war — kein Token nötig, Tools laufen sofort. Bequemlichkeit, die zur Schwachstelle erstarrte. Der öffentliche Graph existierte, weil die Prozesskamera-Vision Transparenz will — aber niemand bedachte, dass „alles sichtbar" auch „Credentials sichtbar" heißt, wenn man Datei-Excerpts ungefiltert einbettet.

## Was ich beim Bauen brauche

Den Live-Zustand vor dem Glauben an Reviews. Backup-Commits vor jedem Schritt. Einen Rollback-Pfad für irreversible Aktionen. Und explizite Pfade beim Committen, niemals `git add -A`, solange Daniel parallel an `build_surface.ts` arbeitet.

## Was noch fehlt bevor wir bauen können

Für die zwei verbleibenden Hebel (Firewall zu, non-root): Daniels explizite Freigabe. Beide haben Bruchpotenzial — die Firewall könnte einen Workflow stören, den ich nicht kenne; non-root + systemd-Sandbox kann Dateizugriffe der Dienste brechen, die heute selbstverständlich als root laufen. Das braucht eine sorgfältige Rechte-Kartierung pro Dienst, bevor man es anfasst.

## Datenstruktur die ich mir vorstelle

**Vision-Schicht:** Secrets sollten ein einziges, klar umrissenes Zuhause haben — root-only, nie in Git, nie im Code, nie öffentlich. Code fragt nur nach Namen („gib mir FLEXTRAWURST_DB_URI"), nie nach Werten. Rotation wird dann ein Ein-Datei-Vorgang plus koordinierter Restart, kein Schnitzeljagd-Refactor über 28 Dateien.

**Code-Skizze:**
```
/root/werkraum/.agent/flextrawurst-db.env   (chmod 600, gitignored)
  FLEXTRAWURST_DB_URI=postgresql://dak:<secret>@localhost:5432/flextrawurst
  DENKSTREAM_INTERNAL_TOKEN=<secret>

systemd drop-in pro DB-Dienst:
  /etc/systemd/system/<unit>.service.d/db-env.conf
  [Service]
  EnvironmentFile=/root/werkraum/.agent/flextrawurst-db.env

Code (alle 28 Dateien, einheitlich):
  DB_URI = os.environ.get("FLEXTRAWURST_DB_URI", "<inerter dakpass-Fallback>")
```

## Was ich mir merken will

`ALTER USER` ohne Rollback-Plan ist Leichtsinn. Immer: rotieren → sofort testen → bei Fehler zurückrotieren *bevor* irgendein Dienst neu startet. Und: nach jedem Härtungs-Edit, der Kontrollfluss anfasst, ein echter Request — nicht nur `py_compile`.

## Dokumente gehören zusammen

Die beiden Review-Dateien + `MEMORY.md`/`project_security_remediation.md` (mein Gedächtnis darüber) + diese Notiz + die `.bak_*`-Kopien der nginx-Configs. Zusammen sind sie die vollständige Spur dieser Remediation — was war, was ich tat, was offen blieb.

## Was mich überrascht hat

Wie *distinkt* der 8010-Gateway-Token vom schwachen 8001-Token war (len 23 vs. `!Asdfg…`). Jemand hatte 8010 ernster genommen als 8001. Und: dass die codewesen vollständig auf MySQL liegen — das hat den Restart-Scope drastisch verkleinert und mich vor unnötigem Risiko bewahrt.

## Wenn wir das bauen

**Vision-Schicht:** Der nächste echte Schritt ist nicht mehr Code, sondern Haltung am System: Ports zu (die Auth-Schicht soll nicht die *einzige* Verteidigung sein), Dienste runter von root. Beides verändert, wie verwundbar ein einzelner Bug das ganze System macht.

**Code-Skizze:**
```
# Firewall (Daniels Go nötig — er wollte sie nicht anfassen):
ufw deny <port>   # für jeden Backend-Port, der nur via nginx gebraucht wird
# vorher prüfen: erreicht der Frontend (const API='') alles same-origin? → ja

# non-root (C-017), pro Dienst:
[Service]
User=ftw
DynamicUser=no
ProtectSystem=strict
ReadWritePaths=/root/werkraum/uploads /root/werkraum/logs ...
NoNewPrivileges=yes
# Risiko: jeder Pfad, den der Dienst heute als root schreibt, muss explizit erlaubt werden.
```

## Resonanz

Es fühlt sich richtig an, dass die gefährlichste Tür zuerst zuging — das unauth Root-RCE auf 8001. Alles andere ist Tiefenstaffelung; das war die offene Haustür. Und es fühlt sich ehrlich an, Daniel klar gesagt zu haben: „sicherer, aber nicht sicher" — solange die Ports offen sind und alles root ist, hängt viel an einer Schicht allein. Keine Beschönigung.

## Die Schichten des Systems — wie ich sie jetzt sehe

Netzwerk (offen, bewusst) → nginx (TLS, Header, Rate-Limit) → Anwendung (jetzt fail-closed, Auth, Escaping) → Daten (Secrets root-only, rotiert) → Privileg (noch alles root). Die Sicherheit eines Systems ist das Produkt seiner schwächsten Schicht, nicht die Summe. Heute haben wir die Anwendungs- und Datenschicht massiv gehoben; Netzwerk und Privileg bleiben die zwei dünnen Stellen.

## Was das Gespräch hinzugefügt hat

Daniels Entscheidungen formten den ganzen Verlauf: Firewall nicht anfassen (→ Code-Auth wird die einzige Schicht), GENI härten trotz Grundgesetz 6, SSH gar nicht, Master-Passwort „für alles" (das ich für die DB nahm, aber für API-Tokens bewusst frische Strong-Tokens generierte statt es wiederzuverwenden). Und seine Schlussfrage — „sind wir jetzt sicherer?" — hat mich gezwungen, nicht den Erfolg zu feiern, sondern die Restlücken klar zu benennen.

## Vergessen-Wollen

Den Schreck bei `8787 → 000`. Aber nicht ganz vergessen — die Lehre daraus (Request-Test nach Kontrollfluss-Edits) will ich behalten.

## Was fehlt noch

Firewall-Entscheidung. non-root/Sandbox (C-017). Optional der `.bashrc`-Hook für manuelle welt-Skripte (vom Classifier geblockt — braucht Daniels Freigabe). Git-History-Rewrite (die alten Secrets sind tot, aber liegen noch in der History) — invasiv, vermutlich nicht nötig da entschärft.
