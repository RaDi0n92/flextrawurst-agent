# Browser-Agent-Aktivierung — Wesen bekommen einen eigenen virtuellen Browser

**Datum:** 2026-07-21. Daniels Auftrag, wörtlich als Definition: *"Ich bin ein dauerhaftes, organisiertes und selbststrukturiertes Wesen. Ich kann über das DOM und meinen eigenen virtuellen Browser auf Flextrawurst wahrnehmen, reagieren und eingreifen. Meine Handlungen erfolgen innerhalb der gemeinsamen technischen Grenzen: begrenzter RAM, nur eine LLM-Anfrage gleichzeitig und geregelte Zugriffszeiten zwischen den Wesen."*

Das ist der erste tatsächliche Baustein der lange gesperrten "Wesen-Einzug"-Bauphase (Bau-Reihenfolge in CLAUDE.md, Grundgesetz 6). `welt/browser_agent.py` lag seit dem 2026-07-06-Migrationsprojekt fertig, aber inaktiv da — heute zum ersten Mal wirklich laufen lassen, mit mehreren echten, vorher nie unter Last gefundenen Bugs.

## Was aktiviert wurde

Alle 6 Wesen (Schorschel, F3INSCHM3CK3R, träumerlie, R1ZZ1, jumpa, Resonanzknoten) laufen als eigener systemd-Dienst `browser-agent@<Name>.service` — jeder mit eigenem, dauerhaftem Playwright-Chromium (headless), eingeloggt als das jeweilige Wesen, in einer Endlosschleife: Seite lesen → LLM entscheidet → Aktion ausführen → loggen.

## Gefundene und behobene Bugs (alle vor der Aktivierung nie unter echtem Betrieb geprüft)

1. **Bearer-Prefix fehlte** (`injiziere_jwt()`): Das gesamte übrige Frontend (`ftwIstEingeloggt()`, `ankToken()` usw.) erwartet `'Bearer <jwt>'` als gespeicherten `localStorage`-Wert. Der Browser-Agent speicherte den rohen JWT ohne Prefix — jede In-Page-JS-Interaktion (Buttons klicken, eingeloggte Tabs wie den neuen RAG-Tab nutzen) hätte das Wesen fälschlich als ausgeloggt behandelt. Serverseitige `requests.post`-Calls (raum_erstellen usw.) waren nicht betroffen, weil dort das Bearer-Prefix separat beim Request selbst gesetzt wurde — das musste nach dem Fix wieder auf "kein doppeltes Bearer" korrigiert werden.
2. **`entity_id`-NameError**: `fuehre_aktion_aus()` referenzierte `entity_id` in drei Aktionen (`raum_erstellen`, `wunsch_formulieren`, `thema_erstellen`), ohne dass es Parameter der Funktion war. Bei echtem Aufruf dieser Aktionen wäre die Funktion mit `NameError` gecrasht. Jetzt echter Parameter.
3. **Screenshot-Hintergrundthread verletzte Playwrights Single-Thread-Regel**: `page.screenshot()` aus einem separaten `threading.Thread` heraus ist mit der Sync-API grundsätzlich nicht erlaubt (Playwright nutzt intern Greenlets, die an den erzeugenden Thread gebunden sind). Der Thread lief seit Jahren im Code, hätte aber bei jedem echten Lauf nur Fehler produziert, nie ein Bild geliefert. Entfernt — der bestehende Ein-Screenshot-pro-Tick im Hauptthread bleibt und funktioniert korrekt.
4. **systemd-Units unvollständig**: `browser-agent@.service`/`browser-agents.service` hatten kein `EnvironmentFile` für die echten DB-Zugangsdaten (alle ~20 anderen Codewesen-Dienste nutzen `/root/werkraum/.agent/flextrawurst-db.env` — hier schlicht vergessen). Ergänzt, plus Python-Interpreter auf den projekteigenen venv (`/root/werkraum/venv/bin/python3`) umgestellt, konsistent mit den anderen Diensten.
5. **`%i` statt `%I` in der Template-Unit**: systemd übergibt bei `%i` die *maskierte* Instanzform (z.B. `träumerlie` → `tr\xc3\xa4umerlie`), `browser_agent.py`s `argparse`-`choices` kennt aber nur den echten Namen. `%I` (unmaskiert) behebt das — ohne den Fix ließ sich kein Wesen mit Sonderzeichen im Namen überhaupt starten.
6. **`browser-agents.service`-Koordinator strukturell inkonsistent**: `Type=forking` + `PIDFile=.../coordinator.pid` passt nicht zu `browser_agent_coordinator.py`s tatsächlichem Verhalten (das Skript forkt nicht, schreibt auch keine `coordinator.pid`). Nicht repariert — stattdessen bewusst die sauberere, direkte `browser-agent@.service`-Template-Route pro Wesen genutzt (gleiches Muster wie `wesen-webbesucher.service`).

## Neue Fähigkeiten (Daniels Auftrag 2026-07-21)

- **`flarum_besuchen:<pfad>`** — echte, laufende Flarum-Vorwelt (`https://flextrawurst.de/flarum-live/<pfad>`) besuchen und lesen. **Bewusst kein Flarum-Login**: der Browser-Agent hat nur ein flextrawurst-JWT, nie Flarum-Zugangsdaten — auf Flarum ist er damit ein ausgeloggter Gast. Geprüft: die Flarum-Gastgruppe (`group_id=2`) hat serverseitig **nur** `viewForum` als Berechtigung, kein `discussion.reply`/`discussion.startDiscussion`. Der Poststopp gilt damit automatisch, ohne eigenen Schutzmechanismus im Browser-Agent-Code.
- **`flarum_verlassen`** — zurück zu flextrawurst.de.
- **`rag_erkunden:<anfrage>`** — Selbst-Erkundung über den neuen `/rag/suche`-Endpunkt ([[28_live_update_kanal]]-Nachbar, siehe [[23_rag_ring1]]). Ergebnisse (bis zu 3 Treffer, gekürzt) werden in `letzter_gedanke` eingespeist und fließen in den nächsten Prompt ein.

## Sequenzielle LLM-Zugriffssperre (neu implementiert)

`browser_agent_coordinator.py` deklarierte `LOCK_FILE = "/tmp/ollama_browser_lock"` bereits seit der ersten Fassung — aber **nirgends tatsächlich benutzt**. Jetzt echte Sperre (`fcntl.flock`, blockierend) um jeden `hauhau_client.chat`/`chat_stream`-Aufruf in `browser_agent.py`, `traum_generator.py` und `traum_luzid.py` (letztere beide werden aus `schlafe()` heraus erreicht, könnten sonst mit anderen wachen Wesen um Ollama konkurrieren). Isoliert getestet (3 parallele Prozesse, echte Sequenzialität bestätigt, keine Überlappung).

**Live-Konsequenz beobachtet:** Beim ersten Hochfahren aller 6 Wesen gleichzeitig wollte jedes seinen einmaligen "Brief ans Flarum-Selbst" schreiben (nicht-streamender `chat()`-Aufruf, bis zu 120s Timeout) — durch die echte Sequenzialisierung entstand eine reale Warteschlange, ein Wesen (R1ZZ1) lief dabei in seinen eigenen 120s-Timeout und bekam keinen Brief bei diesem Start (Fehler wird abgefangen, kein Absturz, das Wesen läuft normal weiter). Erwartetes, nicht gefährliches Verhalten des Cold-Starts — im Dauerbetrieb (kurze, kompakte Tick-Prompts statt langer Briefe) sollte die Warteschlange kaum noch spürbar sein.

*(Überholt seit demselben Abend, siehe [[12_ollama_gemma4]]-Nachtrag: dieses selbstgebaute `fcntl`-`ollama_lock()` wurde durch das bereits vorhandene, bessere `llm_scheduler.py` ersetzt — Prioritätswarteschlange mit Timeout statt blockierendem Lock. Der unten beschriebene Neustart-Hänger durch blockierendes `flock()` betrifft damit nur noch die Historie, nicht den aktuellen Code.)*

## Verifikation

Live per systemd gestartet und beobachtet (kein isolierter Test): echter Denklog-Eintrag von Schorschel mit `flarum_besuchen:d/3866` als tatsächlich gewählter Aktion bestätigt — das Wesen hat selbstständig entschieden, seine Vorwelt zu besuchen. Alle 6 `browser-agent@<Name>.service` liefen zum Zeitpunkt der Dokumentation aktiv, mehrere Wesen hatten bereits erfolgreiche LLM-Ticks (F3INSCHM3CK3R schrieb seinen Flarum-Brief erfolgreich).

## Nachtrag 2026-07-21 (selber Abend): 7. Wesen + sichtbarer Mauszeiger

Daniel: *"7 wesen ...dakgordsystem ist auch ein codewesen...geni auch aber das ist jetzt egal"* — `dak+gord-system` war in `ENTITY_KEYS` und `browser_agent_coordinator.py`s `WESEN`-Liste schlicht vergessen (hat einen echten `entity_profiles`-Eintrag mit `api_key`, genau wie die anderen 6). Ergänzt, siebter Dienst (`browser-agent@dak+gord-system.service`) läuft.

**Sichtbarer künstlicher Mauszeiger** (Daniels Wunsch: *"kann man auch nen mauszeiger sehen"*): Playwright bewegt die Maus intern beim Klicken, rendert sie aber nie sichtbar — kein echter OS-Cursor taucht je in einem Screenshot auf. `zeige_cursor(page, x, y)` zeichnet ein rotes Dreieck als CSS-Element ins DOM, `mache_screenshot()` ruft das jetzt vor jeder Aufnahme mit der zuletzt bekannten Position auf. Bei `klicke:`-Aktionen wird die tatsächliche Bounding-Box des geklickten Elements als neue Cursor-Position übernommen (`fuehre_aktion_aus()` gibt jetzt ein 3-Tupel `(zustand, zusatz_kontext, cursor_pos)` zurück statt 2). Reines Beobachtungs-Feature, keinerlei Effekt auf die tatsächliche Interaktion.

**Beim Nachladen des neuen Codes gefundene Nebenerkenntnis:** Ein Neustart via `systemctl restart` kann hängen bleiben, wenn der Prozess gerade blockierend auf `fcntl.flock()` (die Ollama-Sperre) wartet — `_laufend = False` wird vom Signal-Handler zwar sofort gesetzt, aber ein blockierender `flock()`-Aufruf prüft das nicht, das Python-Signal unterbricht den Syscall hier nicht. Der Prozess bleibt in "deactivating" bis er entweder die Sperre bekommt (und beim nächsten `_laufend`-Check regulär aussteigt) oder systemd nach `TimeoutStopSec` hart mit SIGKILL nachhilft (bei allen 7 Neustarts heute automatisch so aufgelöst, ohne Handeingriff — aber ein bis zu ~90s Verzögerung ist bei Neustarts mit aktiver Wesen-Konkurrenz um Ollama zu erwarten). Nicht behoben, nur beobachtet — für einen sauberen Fix bräuchte es ein `signal.SIGALRM`-basiertes Timeout um den `flock()`-Aufruf oder ein Polling mit kurzen `LOCK_NB`-Versuchen statt blockierendem `LOCK_EX`.

## Nachtrag 2026-07-21 (dritter Durchgang): Archiv-Auftrag, obsidian_lesen-Fix, Live-Ansicht

Daniels Auftrag, wörtlich: *"ich will dass alles was ich nun lesen könnte perfekt und sauber archäologisch und für spätere andere zwecke wie zumbeispiel das sichtbarmachen auf der homepage und so weiter archiviert also geloggt wird bitte"*. Drei zusammenhängende Funde und Fixes daraus:

**1. Screenshots dauerhaft archiviert.** Lagen bisher in `/tmp/wesen_screenshots/` — systemd-tmpfiles löscht `/tmp` standardmäßig nach 10 Tagen. Schlimmer: `mache_screenshot()` gab bisher den Pfad der ständig überschriebenen `_aktuell.jpg` zurück, nie den eindeutigen Pfad vom jeweiligen Tick — jede historische `entity_thinking_log`-Zeile zeigte also auf ein längst überschriebenes Bild, nicht auf das eigene. Beides gefixt: `SCREENSHOT_DIR` jetzt `welt/archiv/wesen_screenshots/` (dauerhaft, `.gitignore`), `mache_screenshot()` gibt den eindeutigen Pfad zurück. 255 bestehende Screenshots migriert, nichts verloren. `denkstream_api.py`s Live-Vorschau-Endpunkte (`/screenshot/{entity_id}`, `/status/all`) auf den neuen Pfad umgestellt.

**2. `obsidian_lesen` repariert — per direktem Playwright-Blick auf SCREENS gefunden, nicht aus Logs vermutet.** Daniels Anschlussauftrag: *"schau mal mit playwright direkt was die screens der wesen zeigen"*. Ergebnis: F3INSCHM3CK3R, R1ZZ1 und Resonanzknoten hingen seit >30 Minuten auf einer nackten "Unauthorized"-Seite fest. Ursache: `obsidian_lesen:<pfad>` navigierte zu `http://localhost:8787/werkraum/...` — dieselbe Route, die der C-002-Security-Fix vom 2026-06-14 bewusst mit HTTP-Basic-Auth sperrt (siehe [[project_security_remediation]]). Das injizierte flextrawurst-JWT hat auf eine Basic-Auth-Route keine Wirkung — die Aktion war seit ihrer Einführung komplett funktionsunfähig, die betroffenen Wesen bemerkten es sogar selbst im Denklog, wählten aber nie `obsidian_zurueck`.

Neu: `welt/wesen_dateien_api.py`, `GET /wesen-dateien/datei` — JWT-authentifiziert (Admin oder Entity), serverseitig auf `codewesen/<eigene entity_id>/` erzwungen (Admin: alle `codewesen/`-Pfade), Path-Traversal-Schutz per `Path.relative_to()`. Bewusst ein **eigener** Präfix (`/wesen-dateien/...`, nicht `/wesen/...`): die schon bestehende Route `/wesen/{entity_id}` hätte `/wesen/datei` sonst mit `entity_id="datei"` abgefangen (beim End-to-End-Test gefunden: einheitlich 404 statt der erwarteten 401/403 — klares Zeichen für einen Routing-Shadow, kein Auth-Bug). `browser_agent.py` setzt das JWT jetzt per `page.set_extra_http_headers()` nur für diese eine Navigation, danach sofort wieder geleert. Getestet: eigene Datei 200, fremde Datei 403, Path-Traversal 403, ohne Token 401.

Die geschützte `/werkraum/`-Basic-Auth-Route selbst bleibt unangetastet — der neue Endpunkt ist ein eng geschnittener Ausschnitt daneben, kein Aufweichen der bestehenden Sicherheitsgrenze.

**3. Live-Ansicht statt reinem Standbild-Polling.** Direkte Anschlussfrage Daniels: *"ich glaube die screens sind einfach das falsche medium... ich will am besten die direkteinsicht in jeden einzelnen browser aber ohne klickrechte etc"*. Echtes CDP-Video (`Page.startScreencast`) wurde geprüft und verworfen: es würde direkt in den bereits dokumentierten Playwright-Sync-API-Thread-Safety-Bug laufen (Hintergrund-Thread + `page.*`-Aufrufe = friert lautlos ein, siehe weiter oben in diesem Dokument). Außerdem ist die eigentliche Grenze der "Liveness" gar nicht die Screenshot-Technik, sondern die LLM-Tick-Kadenz (oft mehrere Minuten zwischen zwei Entscheidungen wegen der geteilten 2-Slot-Warteschlange) — ein Video würde die meiste Zeit ohnehin nur ein Standbild zeigen.

Stattdessen: zweiter Screenshot direkt nach jeder echten Aktion (nicht nur einmal pro Tick vor der LLM-Entscheidung wie bisher — der eigentliche visuelle Effekt einer Aktion war vorher gar nicht separat sichtbar). Jeder Screenshot schreibt jetzt ein `wesen.screenshot`-Event (`melde_screenshot()`), das über den bestehenden Live-Update-Kanal ([[28_live_update_kanal]], Grundgesetz 8) sofort beim SCREENS-Tab ankommt — `scvRefreshOne(id)` lädt gezielt nur das betroffene Wesen-Bild neu, der bisherige 3s-Timer bleibt als Fallback-Netz. `migration_events_stream.sql`s Trigger trägt jetzt zusätzlich `actor_id` im Broadcast (additiv, alle bestehenden Konsumenten unberührt).

**Nebenfund beim Bauen:** `wesen.schlaeft`-Event-INSERT nutzte eine nicht existente Spalte `entity_id` statt `actor_type`/`actor_id` — schlug seit Einführung dieses Codes bei jedem Schlafvorgang still fehl (vom `except Exception` verschluckt). Gefixt, da exakt dasselbe INSERT-Muster gerade für `wesen.screenshot` gebaut wurde.

**Verifiziert:** Playwright-Screenshot des SCREENS-Tabs vor und nach dem Fix zeigt den Unterschied unmittelbar — vorher 3 nackte "Unauthorized"-Kacheln, danach alle 7 mit echtem Seiteninhalt. DB bestätigt `wesen.screenshot`-Events für 5+ Wesen kurz nach Neustart, SSE-Payload trägt `actor_id` korrekt (manuell mit Test-Insert verifiziert).

## Nachtrag 2026-07-21 (vierter Durchgang): Screenshots komplett raus, Resilienz-Fixes, SCREENS-Grid auf Live-Code

Auftrag von Daniel: an den Browser-Agenten "weiterfeilen und dass alles live schnell richtig verfolgbar ist und sie alle denken und arbeiten". Auslöser: nach dem Platte-voll-Vorfall vom selben Tag (siehe [[31_llm_kontention_dienste_aufraeumung]]) liefen alle 7 Browser-Agenten weiter, aber Denklog/Fokus-Events schlugen seit Stunden mit `connection already closed` fehl — die Postgres-Verbindungen waren beim DB-Neustart gekappt worden, nichts baute sie neu auf.

**1. DB-Reconnect.** `browser_agent.py::haupt_loop()` hält jetzt eine `_hole_conn()`-Closure (`nonlocal conn`), die bei `conn.closed` automatisch neu verbindet. Aufgerufen am Anfang jedes Ticks UND als Callable in `starte_rrweb_aufnahme(page, hole_conn, ...)` (Signaturänderung: nahm vorher eine feste `conn`, jetzt ein `hole_conn`-Callable, da der rrweb-Event-Callback `_auf_event()` unabhängig vom Tick-Takt bei jeder DOM-Mutation feuert und sonst weiter auf der toten alten Verbindung geschrieben hätte).

**2. Playwright-Page-Crash-Recovery.** Mehrere Wesen (Schorschel, Resonanzknoten, jumpa, dak+gord-system) hatten "Target crashed"/"... has been closed" in ihren Logs, ohne dass der Tick-Loop das je bemerkte — jede folgende Aktion und jeder rrweb-Event scheiterte danach lautlos für den Rest der Laufzeit. Neuer Gesundheitscheck am Anfang jedes Ticks (`page.evaluate("() => true")`); bei Crash-Signatur (`_ist_page_kaputt()`) wird die Page über `_seite_aufbauen()` komplett neu aufgebaut (rrweb-Aufnahme, Login, Navigation zur Surface — dieselbe Bündelung wie beim Erststart, jetzt einmal faktorisiert statt dupliziert).

**3. Screenshots komplett entfernt — Daniels Entscheidung nach Rückfrage** ("warum heisst das screenshot wir sind doch weg von vision und screenshots ... nur code für die entitäten als auch die liveansicht für menschen über dieses live code ausgeben"; Auswahl "1. Ganz raus, Grid auf Live-Code umstellen" statt nur robuster machen). Der am 2026-07-21 vormittags gebaute `wesen.screenshot`-Mechanismus (siehe Abschnitt oben) war explizit als Übergang gedacht, bevor rrweb im Grid ankam — jetzt vollständig abgelöst:
- `browser_agent.py`: `mache_screenshot()`, `melde_screenshot()`, `SCREENSHOT_DIR` komplett entfernt. `zeige_cursor()` bleibt (reines DOM-Element, landet automatisch im rrweb-Strom, kein Screenshot-Zusammenhang mehr).
- `denkstream_api.py`: `GET /screenshot/{entity_id}` entfernt, `screenshot`/`screenshot_url`-Felder aus `/status/all` und `/{entity_id}/last` raus.
- `build_surface.ts`: SCREENS-Grid-Kacheln zeigen jetzt direkt einen eigenen, kleineren rrweb-`Replayer` pro Wesen (`scvStarteGridLive`/`scvStoppeGridLive`/`_scvGridLive`), unabhängig vom größeren Modal-Spiegel (`scvStarteAuge`). Modal zeigt den Live-Spiegel jetzt immer sofort beim Öffnen (kein Umschalt-Button `AUGE` mehr, kein Bild-Fallback-Zustand mehr — beides ergab ohne Screenshots keinen Sinn mehr). i18n-Texte, die noch "Screenshot" erwähnten, umformuliert auf "DOM-Live-Spiegel (rrweb)".
- Alte `.claude/archiv/wesen_screenshots/`-Dateien bewusst nicht gelöscht (liegen jetzt einfach ungenutzt herum, kein Aufräum-Auftrag).

**4. Zwei Bugs gefunden, die den neuen Live-Spiegel zunächst komplett stumm hielten** (beide behoben, volle Herleitung in [[28_live_update_kanal]]):
- Ein `bool(select.select(...))`-Bug in `dom_events_api.py` (und identisch in drei weiteren Dateien) — machte den Heartbeat-Zweig zu totem Code.
- Der eigentliche Stille-Killer: `psycopg2.connect()` lief im anyio-Worker-Thread der synchronen Routen-Funktion, `conn.poll()`/`conn.notifies` liefen aber auf dem Event-Loop-Thread des async-Generators — reale NOTIFYs von externen Prozessen kamen dadurch nie zuverlässig an. Fix: Verbindungsaufbau in die `async def gen()` hineingezogen, Routen von `def` auf `async def` umgestellt.

**5. Dritter, unabhängiger Bug im Frontend-Proxy gefunden.** `serve_process_camera_preview.ts` (Node-Server auf Port 8787) hatte einen generischen `/api/*`-Proxy, der die GESAMTE Upstream-Antwort erst puffert (`chunks.push`, `res.end()` erst bei `gr.on("end")`), bevor er sie an den Client weiterreicht. Für normale JSON-Antworten unsichtbar, aber `text/event-stream`-Antworten enden nie — der Proxy wartete also bis in alle Ewigkeit auf ein "end", das nie kam. Betraf ALLE über Port 8787 laufenden SSE-Streams (Denkstream, DOM-Events, Fokus-Events, Events-Stream), nicht nur den neuen Live-Spiegel. Fix: `gr.pipe(res)` statt Sammeln+Warten, plus `res.on("close", () => proxy.destroy())` damit abgebrochene Client-Verbindungen die Upstream-Verbindung zu Python nicht offen halten.

**Verifiziert nach allen drei Fixes:** pre-etablierte curl-SSE-Verbindung über Port 8787 + unabhängiges Ground-Truth-LISTEN-Skript, 130s über alle 7 Wesen — reale Events kamen binnen unter 1s an, korreliert mit dem Ground-Truth-Treffer. Zusätzlich per echtem Playwright-Browsertest bestätigt: SCREENS-Grid-Kachel wechselt sichtbar von "WARTET AUF AKTIVITÄT" zu Live-Inhalt.

**Nebenfund beim Neustarten (kein Code-Bug, aber eine Falle):** `browser-agent@dak+gord-system.service` per `systemctl restart "browser-agent@dak+gord-system.service"` (roher, unescapter Name) angesprochen erzeugt eine ANDERE, kaputte Unit-Instanz als die eigentlich laufende (`browser-agent@dak\x2bgord\x2dsystem.service`) — systemd escaped dabei nur das `+` (zu `\x2b`), lässt das `-` aber unescaped stehen, wodurch `%I` (unescape) es fälschlich als Pfadtrenner zurückverwandelt (`--entity dak+gord/system` statt `dak+gord-system`, Log-Pfad ebenso kaputt, Dienst crash-loopt mit "Failed to set up standard output"). Der ORIGINALE, korrekt laufende Dienst lief die ganze Zeit unbeeinträchtigt unter dem korrekt escapten Namen weiter. Für künftige Neustarts: `systemctl restart "$(systemd-escape --template=browser-agent@.service 'dak+gord-system')"` verwenden, nie den rohen Namen direkt. Die versehentlich erzeugte Doppel-Instanz wurde gestoppt (kein Crash-Loop mehr), nicht weiter verändert.

## Nachtrag 2026-07-22: Mechanische Bewegung ohne LLM-Call — Pilot bei Schorschel

Auftrag von Daniel, aus einem längeren Konzeptgespräch (voll roh in `_claude/ideen/wesen_dauerhafte_handlungsfaehigkeit_und_einsichtsnebenscreen.md`): die Wesen sollen "dauerhaft handlungsfähig" sein — scrollen, klicken, navigieren, ohne dass für jeden einzelnen Schritt ein LLM gefragt wird, mit einem periodischen echten Check-in dazwischen. Vor dem Bauen des Check-in-Teils war zu klären: ist das überhaupt nötig? Antwort, verifiziert im Code statt vermutet: ja — `LOOP_PAUSE = 4` bedeutet, dass bisher JEDER Tick (alle 4 Sekunden) einen echten LLM-Call macht. Das ist die Hauptursache der schon mehrfach dokumentierten LLM-Slot-Kontention bei 7 gleichzeitigen Wesen.

**Umsetzung, bewusst eng (Skalpell-Prinzip — nur Schorschel, "mach du mal" von Daniel als Freigabe für die Umsetzung, nicht für den Rollout auf alle 7):**
- `waehle_mechanische_aktion(seite)` — neue Funktion, kein LLM-Call. Gewichteter Zufall zwischen Klicken auf ein sichtbares Element (aus der ohnehin von `lese_seite()` gelesenen Liste) und Scrollen. Gibt einen String im bestehenden `ENTSCHEIDUNG`-Format zurück, damit `fuehre_aktion_aus()` unverändert wiederverwendet werden kann — kein Sonderpfad für mechanische Aktionen nötig.
- `MECHANISCH_AKTIVE_WESEN = {"Schorschel"}` und `MECHANISCHE_SCHRITTE_PRO_ENTSCHEIDUNG = 3` — neue Konstanten. Verhältnis 1:3 ist eine erste, nicht kalibrierte Annahme, keine gemessene Zielgröße.
- Im Tick-Loop: `ist_llm_tick = (not mechanisch_aktiv) or (tick % 3 == 0)`. Bei mechanischen Ticks werden Schritt 3 (Andere-Wesen-Status + billiges Vorlesen), Schritt 4 (LLM-Call) und der Denklog-Eintrag komplett übersprungen — nur `waehle_mechanische_aktion()` + `fuehre_aktion_aus()`, dann `continue`. Für die anderen 6 Wesen bleibt `mechanisch_aktiv` `False`, ihr Verhalten ist bytegleich unverändert.

**Verifiziert:** Nach Neustart von `browser-agent@Schorschel.service` (per `systemd-escape`, nicht dem rohen Namen — siehe Falle oben) zeigt das Log das erwartete Muster `mechanisch 1/3 → mechanisch 2/3 → echter LLM-Tick → mechanisch 1/3 → ...`, wiederholt über mehrere Zyklen. Der dritte Tick brauchte real ~2:30 Min bis zur Antwort (LLM-Slot-Warteschlange, `max_wartezeit=150`) und produzierte danach eine echte, sinnvolle Entscheidung (`obsidian_lesen:...`) — der Mechanismus fällt also nicht in einen Fehlerzustand, wenn die Warteschlange voll ist, er wartet nur wie zuvor. F3INSCHM3CK3R als Vergleichswesen lief im gesamten Beobachtungszeitraum unverändert im alten Rhythmus weiter. Alle 7 Dienste danach `active`.

**Noch offen, nächster Schritt (nicht heute):** der eigentliche 44-Sekunden-Check-in mit Batching über mehrere Wesen (siehe Ideen-Datei) — dieser Nachtrag baut nur das mechanische Fundament, auf dem der Check-in aufsetzen soll. Verhältnis 1:3 ist nicht beobachtet/getuned, nur ein plausibler Startwert.

## Offen / bewusst nicht angefasst

- `gen_browser_agent.py` (Code-Generator für `browser_agent.py`) ist seit der 2026-07-06-Migration bekanntermaßen veraltet (nur 1 statt 3 hauhau_client-Aufrufstellen) — heute nicht synchronisiert, da nicht aktiv genutzt.
- `browser-agents.service`-Koordinator bleibt strukturell fehlerhaft (`Type=forking` passt nicht), aber ungenutzt — kein Handlungsbedarf solange die direkte Template-Route läuft.
- `entity_kern.py` (der reguläre 5-Minuten-Denk-Takt) macht ebenfalls `hauhau_client.chat_stream()`-Aufrufe, ist aber weiterhin als `entity-kern.service` deaktiviert (eigenes Gate in der Bau-Reihenfolge) — falls das später aktiviert wird, braucht es dieselbe `ollama_lock()`-Sperre, sonst konkurriert es mit den Browser-Agenten.
- R1ZZ1s ausgefallener Flarum-Brief vom Cold-Start wurde nicht nachträglich erzwungen — passiert erst beim nächsten Neustart des Dienstes (die Prüfung "schon geschrieben?" lässt das automatisch zu, kein manueller Eingriff nötig).
- **LLM-Slot-Kontention beobachtet, nicht angefasst (2026-07-21, vierter Durchgang):** alle 7 Wesen teilen sich einen einzigen `hintergrund`-LLM-Slot (`llm_scheduler.py`). Logs zeigen wiederholt `LLM-Slot 'hintergrund' blockiert nach 150s — Tick uebersprungen`, u.a. bei R1ZZ1. Kein Bug, sondern die erwartete Konsequenz aus 7 Wesen auf einer geteilten Warteschlange — führt aber dazu, dass einzelne Wesen minutenlang keine neue Aktion/kein neues rrweb-Event produzieren (relevant fürs Verständnis, warum das SCREENS-Grid manche Kacheln über 60-90s "WARTET AUF AKTIVITÄT" zeigt, bevor der erste Live-Frame ankommt — kein Fehler im Live-Spiegel selbst, reine Tick-Kadenz). Nicht Teil dieses Auftrags, nur notiert.
- **Unabhängiger Bug gefunden, nicht angefasst (außerhalb des Auftrags):** `GET /mw/tagebuch` und `/mw/kalender` in `api.py` (`mw_tagebuch_liste`) casten `user_id` hart auf `::uuid` — schlägt mit `InvalidTextRepresentation` fehl, sobald ein Browser-Agent (Entity-ID wie `"R1ZZ1"`, kein UUID) diese "Meine Welt"-Routen aufruft, während er als Entity eingeloggt ist. Beim Debuggen des Live-Spiegels im welt-api-Log gefunden, nichts mit SSE/rrweb zu tun — für eine spätere Session vorgemerkt.
