Lesen/Schreiben — geprüft, beides ist live, kein Umweg über einen Spiegel: - Lesen (Diskussionsauswahl in engagement.py): läuft über flarumapi.getrecentdiscussions() / getdiscussion() / getunanswereddiscussions() — direkte pymysql.connect()-Verbindung zur echten Flarum-MySQL-DB, live, kein Cache. Es gibt zwar noch eine ladeallediskussionen()-Funktion, die den 5-Minuten-Cron-Vault-Spiegel (scripts/flarumsync.py → /root/werkraum/flarum/) liest — aber die wird im ganzen File nirgends mehr aufgerufen. Toter Code, nicht die Ursache. - Schreiben: flarumapi.postreply() / start_discussion() nutzen requests.post() gegen die echte Flarum-HTTP-API (http://217.154.14.29/api) — ebenfalls live, keine MySQL-Direktschreibung, kein Vault-Umweg. Damit ist deine Vermutung geprüft und widerlegt: es liegt nicht an einem veralteten Zwischenspeicher beim Lesen oder Schreiben. Beide Wege sind live — das Problem sitzt tatsächlich nur an der einen Stelle, die wir schon gefunden haben: dem gemeinsamen LLM-Slot. Zu "warten muss deutlich höher, Timeout auch": Das passt genau zur gemessenen Ursache — maxwartezeit=90 in codewesenantwortaufdaniel.py:202 ist zu kurz, wenn andere PRIOHOCH-Halter bis zu 600s brauchen. Genau dieselbe Lektion wurde am 09.07. schon für umgekehrteneugier gezogen (dort auf 3600s angehoben, siehe docs/systemdoku/20flarumstopp.md). Ich hab die Schleife in main() geprüft: es ist ein einfacher while True: tick(); sleep(...)-Daemon, kein Cron, kein Overlap-Risiko — ein längeres Warten verzögert nur den nächsten Tick, verursacht aber keine Kollision mit sich selbst. Von der Architektur her ist eine Anhebung also unbedenklich. Bevor ich das ändere — kurz bestätigen, damit ich nicht rate: Ich würde in codewesenantwortaufdaniel.py:202-203 maxwartezeit=90 und maxhaltezeit=600 deutlich anheben (z.B. auf 600s Warten, angelehnt an die real gemessenen Haltezeiten der Konkurrenz) und den hauhauclient.chat(..., timeout=600.0)-Wert entsprechend mit hochziehen, falls er die Begrenzung wäre. Nur diese eine Datei/Stelle, oder soll ich codewesenreaktion.py (dieselbe PRIOHOCH/90s-Konstellation bei inbox_antwort) gleich mit anfassen?
