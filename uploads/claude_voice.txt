Läuft wieder — Obsidian ist voll da, indiziert gerade den Vault ("28_partner" Notiz offen). Root Cause war ein zu knappes V8-Heap-Limit (1 GB) im Image-Standard-Wrapper, das bei jedem Neustart durch den großen Vault sofort gesprengt wurde und den Renderer in einer Crash-Restart-Schleife gefangen hat, ohne je zu malen. Behoben durch: - Heap-Limit im Start-Wrapper /usr/bin/obsidian von 1024 MB auf 4096 MB angehoben - --disable-gpu-compositing gegen --enable-software-rasterizer getauscht (Software-Rendering, da kein GPU-Passthrough) Kannst du bei dir im Browser nochmal auf https://217.154.14.29:8443/ schauen, ob's auch dort läuft? Falls ja, sollte ich das noch als Notiz in _claude/notizen/ dokumentieren (Grundgesetz Doku-Pflicht) — die Änderung liegt aktuell nur im laufenden Container, nicht in einem Compose-File, geht also bei einem docker rm/Recreate wieder verloren, falls es dafür eins gibt.
