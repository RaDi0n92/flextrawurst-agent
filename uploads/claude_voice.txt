Fertig. Was jetzt geht: Ghost-Sessions — 👻 in der Sidebar startet sofort eine neue Geister-Session mit Namens-Popup. Sie erscheint in der Sidebar lila/dunkel mit 👻 prefix, getrennt von den echten Sessions. Aktionen: → sys (materialisieren), ↓ .md (client-seitig runterladen), vergessen (aus dem Browser-Speicher löschen). Beim Wechsel in eine andere Session wird der aktuelle Stand gespeichert. Auf Reload sind Geister-Sessions weg — das ist der Sinn. Session-Rename — Feld + ✓ Button. Enter oder ✓ schickt den Namen ans System. Kein stilles Debounce mehr. -N ctx — Klick auf -N ctx öffnet ein Modal: alle Nachrichten aufgelistet, die rot markierten fallen weg, Slider steuert wie viele, Token-Freigabe und Restbestand sind sichtbar. Erst nach Bestätigung wird geschnitten. Kontext einspeisen — Neuer Mischpult-Abschnitt "Kontext einspeisen". Freitextfeld → → als Kontext schreibt es als [kontext: (eingabe)] ins msgs-Array. ↑ .md(s) lädt eine oder mehrere Dateien in der Upload-Reihenfolge nacheinander ein. Tokenzahl wird angezeigt. Nichts wird gesendet — nur der Kontext wächst. Gelöschtes Feld — kannst du mir sagen welches es war? Bei Reload der Seite kommen alle 5 Standard-Felder (ton/fokus/tempo/haltung/kontext) automatisch zurück.
