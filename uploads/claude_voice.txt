Verstanden — zwei Teile: 1. Die Pipeline selbst ist schon so, wie du sie willst. Ich hab bearbeitepost() nochmal geprüft: kein Container, kein Ready-Check, keine Sammel-Logik. Post von dir rein → fragellm() → direkt flarumapi.postreply()/startdiscussion() (nicht mal über den flarumposter.poster()-Umweg der anderen Dienste). Lesen — Schreiben — Ende — API. Genau dein Bild. Das ganze Container/Ready-Check-Zeug von eben betrifft nur forum_neugier/takt/engagement/reflexion — nie diesen Dienst. Hier ist nichts zu ändern. 2. "Eigenständig, dass er allein trägt" — und du hast recht mit deiner Vermutung: das Problem ist nicht die Pipeline, sondern dass dieser Dienst sich den einen gemeinsamen hintergrund-LLM-Slot (Port 11436) mit allen 15 anderen Background-Diensten teilt (Agent-Loops, Reaktion, Engagement, Batch-Generator, lgdaemon, umgekehrteneugier, ...) — genau die Warteschlange, die wir die ganze Zeit besprochen haben. Es gibt aber schon eine zweite, komplett getrennte llama-Instanz: Port 11435, llama-hauhaucs.service — bisher exklusiv für die 5 echten Live-Chat-Einstiegspunkte reserviert (codewesenchat.py, geni/dialog.py, zensi/server.py, die beiden Chat-UIs), über idslot=0 (gebaut am 06.07., genau der Tag von dem du eben gesprochen hast). Wer id_slot=0 übergibt, bekommt sofort einen eigenen Slot, ganz ohne die hintergrund-Warteschlange zu berühren. Vorschlag: codewesenantwortaufdaniel.py auf idslot=0 + llm_scheduler.LLMSlot(server="chat", ...) umstellen — dann läuft es nicht mehr gegen die 15 Hintergrund-Dienste, sondern nur noch gegen die paar echten Live-Chats (selten, kurz). Eine dritte eigene llama-Instanz wäre dagegen keine gute Idee — aus dem Bericht vom 06.07. wisst ihr schon, dass mehr Parallel-Slots bei diesem MoE-Modell auf CPU-only die Geschwindigkeit kaputt machen (--parallel 3 wurde deshalb verworfen), eine dritte Instanz würde sich zusätzlich dieselben CPU-Kerne mit den anderen beiden teilen. Das ist eine Architektur-Entscheidung (welcher Pool/welche Priorität) — bevor ich das umbaue: passt das so, oder sollen wir es anders lösen?
