Fix #3 (systemd-oomd) umgesetzt. - Paket installiert (war komplett nicht vorhanden), Service läuft und ist enabled. - PSI (Pressure Stall Information) im Kernel verfügbar — Voraussetzung, dass oomd überhaupt greifen kann. Bestätigt, funktioniert. - Schwellwerte: SwapUsedLimit=85% (statt Kernel-Default 90% — wir wollen früher reagieren als gestern, wo es bis 100% ging), DefaultMemoryPressureLimit=60%, DefaultMemoryPressureDurationSec=20s. - system.slice (alle System-Dienste) auf ManagedOOMSwap=kill + ManagedOOMMemoryPressure=kill gesetzt — das war zuvor komplett inaktiv (auto ohne Wirkung), oomd hätte gestern gar nicht eingegriffen. - PostgreSQL + MySQL explizit auf ManagedOOMPreference=avoid — die werden nur als allerletztes geopfert, damit keine DB-Writes mitten drin abreißen. Beobachtung nebenbei: Swap ist in der letzten Stunde von 3,6 GB auf 6,3 GB gewachsen (57%), trotz deaktiviertem LLM-Cache. Noch unter der 85%-Schwelle, aber im Auge behalten — das gehört zu Punkt 4. Weiter mit dem Swap-Alarm?
